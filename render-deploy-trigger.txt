Churvox Render deploy trigger

Triggered at: 2026-05-30 NZT
Change: fix frontend CSS/JS MIME serving

Frontend:
- adds custom static server with correct Content-Type for CSS and JS
- disables stale index.html caching
- prevents missing /static assets from falling back to index.html
- keeps SPA route fallback for app routes
- backend untouched
