# Churvox fresh backup files

These were old FreshShell backup snapshots that lived inside active frontend source.

They were moved out of `frontend/src/churvox-fresh` so launch greps and wording checks do not keep reading old unused route/accounting wording.

Do not import these files into the app.
