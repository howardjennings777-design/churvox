Churvox whole-site ready pass
Generated: 2026-06-26T16:50:10Z
Site: https://www.churvox.com
API: https://grassley-backend.onrender.com

Promise being tested: Job done. Admin prepared. Owner approves.

===== A) Core app files exist =====
[PASS] App shell / navigation — frontend/src/churvox-fresh/FreshShell.jsx
[PASS] Plan rules / navigation gating — frontend/src/churvox-fresh/planRules.js
[PASS] Command operating system — frontend/src/churvox-fresh/FreshCommandOperatingSystem.jsx
[PASS] Owner Health Check layer — frontend/src/churvox-fresh/FreshTopNineOperatingLayer.jsx
[PASS] Owner Health Check styles — frontend/src/churvox-fresh/freshTopNineOperatingLayer.css
[PASS] Jobs page — frontend/src/churvox-fresh/FreshJobs.jsx
[PASS] Clients page — frontend/src/churvox-fresh/FreshClients.jsx
[PASS] Quotes page — frontend/src/churvox-fresh/FreshQuotes.jsx
[PASS] Invoices page — frontend/src/churvox-fresh/FreshInvoices.jsx
[PASS] Team page — frontend/src/churvox-fresh/FreshTeam.jsx
[PASS] Worker proof / worker command — frontend/src/churvox-fresh/FreshWorkerCommand.jsx
[PASS] Plans page — frontend/src/churvox-fresh/FreshPlans.jsx
[PASS] Imports page — frontend/src/churvox-fresh/FreshImports.jsx
[PASS] Support / help page — frontend/src/churvox-fresh/FreshHelpDesk.jsx
[PASS] Accounting sync / Xero page — frontend/src/churvox-fresh/FreshXero.jsx
[PASS] Backend server — backend/server.py
[PASS] Timer backend patch — backend/churvox_job_timer_routes_patch.py

===== B) Product flow checks in source =====
[PASS] Smart Hub labels exist — found
frontend/src/churvox-fresh/README.md:11:- Smart Hub wording is retired.
frontend/src/churvox-fresh/FreshPlans.jsx:32:    includes: ["Jobs, clients, quotes and invoices", "Smart Hub", "Basic Command desk", "250 clients", "50 jobs/month", "Accounting Sync Add-on available"],
frontend/src/churvox-fresh/planRules.js:48:  planday: { area: "Smart Hub", open: "start", reason: "Every plan gets the daily owner cockpit for jobs, admin, priorities and follow-ups." },
frontend/src/churvox-fresh/planRules.js:95:  { title: "Start", items: [["planday", "PD", "Smart Hub"], ["command", "CM", "Command"]] },
frontend/src/churvox-fresh/planRules.js:101:  { title: "Start", items: [["planday", "PD", "Smart Hub"], ["command", "CM", "Command"], ["messages", "MS", "Messages"]] },
frontend/src/churvox-fresh/planRules.js:108:  { title: "Start", items: [["planday", "PD", "Smart Hub"], ["command", "CM", "Command"], ["messages", "MS", "Messages"]] },
frontend/src/churvox-fresh/planRules.js:116:  { title: "Start", items: [["planday", "PD", "Smart Hub"], ["command", "CM", "Command"], ["messages", "MS", "Messages"]] },
frontend/src/churvox-fresh/planRules.js:135:export const MOBILE_ITEMS = [["planday", "PD", "Smart Hub"], ["jobs", "JB", "Jobs"], ["command", "CM", "Command"], ["invoices", "$", "Money"], ["more", "+", "More"]];
[PASS] Mobile quick actions exist — found
106:  function openMobileJob() { const text = "New job"; try { window.localStorage.setItem(OPEN_JOB_MODAL_KEY, JSON.stringify({ open: true, instruction: text, text, at: Date.now() })); } catch {} if (currentPrimary === "jobs") fireJobAsk(text); else { go("jobs"); [120, 350, 800].forEach((delay) => window.setTimeout(() => fireJobAsk(text), delay)); } }
135:          <section className="freshMobileQuickActions" aria-label="Mobile quick actions"><button type="button" aria-label="New job" title="New job" onClick={openMobileJob}><b>+</b><span>New job</span></button><button type="button" aria-label="Add client" title="Add client" onClick={openMobileClient}><b>CL</b><span>Add client</span></button><button type="button" aria-label="Open approvals" title="Open approvals" onClick={() => go("command")}><b>OK</b><span>Approve</span></button>{canOpenMessages ? <button type="button" aria-label="Open messages" title="Open messages" onClick={() => go("messages")}><b>MS</b><span>Messages</span></button> : null}</section>
[PASS] Owner approval language exists — found
frontend/src/churvox-fresh/FreshAiUsage.jsx:10:    action: "Prepared owner approval slips",
frontend/src/churvox-fresh/FreshAiUsage.jsx:16:    note: "Core Churvox promise: Churvox does the admin. You approve.",
frontend/src/churvox-fresh/FreshAiUsage.jsx:27:    note: "Keep reminders owner-approved before customers receive anything.",
frontend/src/churvox-fresh/FreshTodaysWork.jsx:293:    if (clean.includes("command") || clean.includes("approve") || clean.includes("review")) {
frontend/src/churvox-fresh/FreshImports.jsx:168:    const slip = { id: `import-${Date.now()}`, group: "Imports", title: `${summary.type} CSV import review`, info: `${summary.imported} imported · ${summary.failed} failed · ${summary.blocked} blocked`, urgency: summary.failed || summary.blocked ? "Review needed" : "Ready", found: `Checked ${summary.total} rows.`, prepared: "Clean rows were imported only after owner approval. Bad rows stayed out of live data.", why: summary.message, owner: "Review failed rows, fix the CSV, or open the connected area.", area: "Data Import", page: "imports", fromInbox: true, createdAt: new Date().toLocaleTimeString([], { hour: "2-digit", minute: "2-digit" }) };
frontend/src/churvox-fresh/FreshImports.jsx:222:    <div className="freshImportsHero"><div><span>CSV import / migration</span><h1>Import real business data without making a mess</h1><p>Upload a CSV, validate rows, then approve clean rows into live Clients, Team, Jobs, Quotes or Invoices.</p></div><div className="freshImportsStats"><div><b>{rows.length}</b><small>checked</small></div><div><b>{clean}</b><small>ready</small></div><div><b>{blocked}</b><small>blocked</small></div><div><b>{imported}</b><small>imported</small></div></div></div>
frontend/src/churvox-fresh/FreshImports.jsx:225:    <div className="freshImportsLayout"><aside className="freshImportsList"><header><div><b>CSV rows</b><span>{clean} ready · {blocked} blocked</span></div></header>{visible.map((r) => <button type="button" key={r.id} className={selected?.id === r.id ? "active" : ""} onClick={() => setSelectedId(r.id)}><b>Row {r.rowNumber}: {r.label}</b><span>{r.type} · {r.status}</span><small>{r.result ? r.result.success ? `Imported${r.result.id ? ` · ${r.result.id}` : ""}` : `Failed: ${r.result.detail}` : r.errors[0] || r.warnings[0] || "Ready"}</small></button>)}{!rows.length ? <div className="freshImportsEmpty"><b>No CSV loaded yet</b><span>Choose a type, then upload a CSV.</span></div> : null}</aside>{selected ? <article className="freshImportsDetail"><div className="freshImportsHead"><div><span>{selected.result ? selected.result.success ? "Imported" : "Failed" : selected.status}</span><h2>Row {selected.rowNumber}: {selected.label}</h2><p>{selected.type} import · {cfg.endpoints.join(" → ")}</p></div><div className="freshImportsHeadActions"><button type="button" onClick={() => openArea(selected.type)}>Open Area</button><button type="button" onClick={() => onNavigate?.("command")}>Open Command</button></div></div><div className="freshImportsCards"><section><span>Validation</span><b>{selected.errors.length ? "Blocked" : "Ready"}</b><p>{selected.errors.length ? selected.errors.join(" · ") : selected.warnings.join(" · ") || "No blocking issues found."}</p></section><section><span>Import result</span><b>{selected.result ? selected.result.success ? "Imported" : "Failed" : "Not imported"}</b><p>{selected.result?.detail || "Clean rows are imported only after owner approval."}</p></section><section><span>Owner control</span><b>{selected.type}</b><p>Bad rows stay out of live data. Fix the CSV and upload again if needed.</p></section></div><div className="freshImportsForm">{Object.entries(selected.payload || {}).slice(0, 16).map(([k, v]) => <label key={k} className={typeof v === "object" ? "wide" : ""}><span>{k}</span>{typeof v === "object" ? <textarea readOnly value={JSON.stringify(v, null, 2)} /> : <input readOnly value={v ?? ""} />}</label>)}</div></article> : null}</div>
frontend/src/churvox-fresh/FreshAiQuoteBuilder.jsx:40:    terms: "Price includes labour and standard tidy. Extra green waste, hidden rubbish or extra time can be approved before invoicing.",
[PASS] Worker proof language exists — found
frontend/src/churvox-fresh/FreshJobs.jsx:235:            <section className="freshStoryLinks"><article><b>{related.quotes.length}</b><span>linked quote{related.quotes.length === 1 ? "" : "s"}</span></article><article className={selectedNeedsInvoice ? "need" : ""}><b>{related.invoices.length}</b><span>{selectedNeedsInvoice ? "needs invoice" : `linked invoice${related.invoices.length === 1 ? "" : "s"}`}</span></article><article><b>{photoCount(selected)}</b><span>proof photo{photoCount(selected) === 1 ? "" : "s"}</span></article><article><b>{related.workers.length}</b><span>worker match</span></article></section>
frontend/src/churvox-fresh/FreshBusinessHealth.jsx:28:    title: "2 workers have not acknowledged jobs",
frontend/src/churvox-fresh/FreshBusinessHealth.jsx:31:    found: "Assigned jobs are not acknowledged by workers.",
frontend/src/churvox-fresh/FreshAudit.jsx:17:    ownerNote: "Good proof trail for customer and business records.",
frontend/src/churvox-fresh/FreshAudit.jsx:152:          <h1>Keep a clean proof trail of every important action</h1>
frontend/src/churvox-fresh/FreshWorkerCommand.jsx:120:function isAcknowledged(job) {
frontend/src/churvox-fresh/FreshWorkerCommand.jsx:121:  return Boolean(pick(job, "acknowledged_at", "worker_acknowledged_at")) || ["acknowledged", "in_progress", "paused", "started", "completed"].includes(statusOf(job));
frontend/src/churvox-fresh/FreshWorkerCommand.jsx:325:  if (!isAcknowledged(job)) return "Acknowledge";
[PASS] Invoice-ready/admin debt language exists — found
frontend/src/churvox-fresh/FreshBusinessHealth.jsx:8:    title: "$780 ready to invoice",
frontend/src/churvox-fresh/FreshAiQuickCreateBrainV6.jsx:175:  async function loadBatchJobs() { const res = await get("/jobs", { params: { status: "completed" }, timeout: 15000 }); const jobs = Array.isArray(res?.data) ? res.data : []; const matches = jobs.filter((job) => jobAmount(job) > 0 && !isAlreadyInvoiced(job)).slice(0, 25).map(matchFromJob); const body = { batch: true, matches, bestMatch: matches[0] || null, ambiguity: matches.length ? "none" : "no_match", previewTitle: matches.length ? "Completed jobs ready to invoice" : "No completed priced jobs ready", previewLines: matches.length ? [`${matches.length} job(s) ready.`, `Draft total ${money(total(matches, jobAmount))}.`, "Draft only — no sending or accounting sync."] : ["No completed jobs with prices found."] }; setLive(body); return body; }
frontend/src/churvox-fresh/FreshProfit.jsx:15:    note: "Good margin. Ready to invoice once photos are checked.",
frontend/src/churvox-fresh/FreshAiQuickCreateBrainV7.jsx:197:  async function loadBatchJobs() { const res = await get("/jobs", { params: { status: "completed" }, timeout: 15000 }); const jobs = Array.isArray(res?.data) ? res.data : []; const matches = jobs.filter((job) => jobAmount(job) > 0 && !isAlreadyInvoiced(job)).slice(0, 25).map(matchFromJob); const body = { batch: true, matches, bestMatch: matches[0] || null, ambiguity: matches.length ? "none" : "no_match", previewTitle: matches.length ? "Completed jobs ready to invoice" : "No completed priced jobs ready", previewLines: matches.length ? [`${matches.length} job(s) ready.`, `Draft total ${money(total(matches, jobAmount))}.`, "Draft only — no sending or accounting sync."] : ["No completed jobs with prices found."] }; setLive(body); return body; }
frontend/src/churvox-fresh/FreshApp.jsx:244:  if (/invoice|bill|charge|money|admin debt/.test(value)) return "invoices";
frontend/src/churvox-fresh/FreshInvoices.jsx:205:      {sourceJob ? <section className="freshJobsInvoiceAlert"><strong>Invoice from completed job</strong><p>{sourceJob.title || "Selected job"} for {sourceJob.client || "customer"} is ready to invoice. Price: {sourceJob.price || "No price saved"}. Check the draft before saving.</p><div className="freshActions"><button type="button" className="freshPrimary" onClick={openInvoicePopup}>Create draft invoice</button><button type="button" className="freshGhost" onClick={clearSourceJob}>Clear</button></div></section> : null}
frontend/src/churvox-fresh/FreshAiOperatorStudio.jsx:83:    found: "$780 ready to invoice, 3 quotes need follow-up, 2 workers have not acknowledged jobs.",
frontend/src/churvox-fresh/FreshAiQuickCreateBrainV5.jsx:134:  async function loadBatchJobs() { const res = await get("/jobs", { params: { status: "completed" }, timeout: 15000 }); const jobs = Array.isArray(res?.data) ? res.data : []; const matches = jobs.filter((job) => jobAmount(job) > 0 && !isAlreadyInvoiced(job)).slice(0, 25).map(matchFromJob); const body = { batch: true, matches, bestMatch: matches[0] || null, ambiguity: matches.length ? "none" : "no_match", previewTitle: matches.length ? "Completed jobs ready to invoice" : "No completed priced jobs ready", previewLines: matches.length ? [`${matches.length} job(s) ready.`, `Draft total ${money(total(matches, jobAmount))}.`, "Draft only — no sending or accounting sync."] : ["No completed jobs with prices found."] }; setLive(body); return body; }
[PASS] Accounting sync owner wording exists — found
frontend/src/churvox-fresh/FreshXero.jsx:208:      <div className={`freshXeroNotice ${connected ? "proper" : "need"}`}><b>{connected ? "Xero connected" : "Xero not connected"}</b><span>{connected ? `Connected to ${connection?.tenant_name || "a Xero organisation"}.` : "Connect Xero after the credentials, redirect URI, scopes, and Accounting Sync add-on are ready."}</span></div>
frontend/src/churvox-fresh/FreshAiUsage.jsx:27:    note: "Keep reminders owner-approved before customers receive anything.",
frontend/src/churvox-fresh/FreshXero.before-compact-header.jsx:83:      why: message || "Accounting sync should be the final controlled step after invoice/payment review.",
frontend/src/churvox-fresh/FreshXero.before-compact-header.jsx:248:            : "Connect is blocked until Render env vars are ready and the Accounting Sync add-on is active for this business."}
frontend/src/churvox-fresh/FreshQuoteAI.jsx:14:      why: "Quotes affect customer price and stay owner-approved.",
frontend/src/churvox-fresh/FreshContracts.jsx:171:          <p>Track quote terms, recurring service rules, subcontractor agreements, payment wording and owner-approved customer conditions.</p>
frontend/src/churvox-fresh/FreshPayments.jsx:222:          <div className="freshPaymentsActions"><button type="button" onClick={() => updateInvoicePayment(selected, { status: "overdue", payment_status: "overdue" })} disabled={busy === "update"}>Mark overdue</button><button type="button" onClick={refreshAccountingPayments} disabled={busy === "accounting"}>Refresh accounting</button><button type="button" onClick={() => onNavigate?.("xero")}>Xero draft sync</button></div>
frontend/src/churvox-fresh/FreshXero.before-scroll-fix.jsx:83:      why: message || "Accounting sync should be the final controlled step after invoice/payment review.",

===== C) Customer-facing wording cleanup =====
[PASS] No builder-facing Top 9 title — no matches
[PASS] No public launch wording in Owner Health UI — no matches
[PASS] No old visible MYOB sales wording — no matches
[PASS] No old non-Xero-page Xero sync wording — no matches

===== D) CSV/import readiness =====
[PASS] CSV template — frontend/public/import-templates/churvox-clients-template.csv
[PASS] CSV template — frontend/public/import-templates/churvox-team-template.csv
[PASS] CSV template — frontend/public/import-templates/churvox-jobs-template.csv
[PASS] CSV template — frontend/public/import-templates/churvox-quotes-template.csv
[PASS] CSV template — frontend/public/import-templates/churvox-invoices-template.csv

===== E) Backend compile =====
[PASS] Backend compile — key backend files compile

===== F) Live route smoke =====
[PASS] Main site — HTTP 200
[PASS] Dashboard / Smart Hub route — HTTP 200
[PASS] Plans route — HTTP 200
[PASS] Clients CSV live — HTTP 200
[PASS] Team CSV live — HTTP 200
[PASS] Jobs CSV live — HTTP 200
[PASS] Quotes CSV live — HTTP 200
[PASS] Invoices CSV live — HTTP 200
[PASS] Auth/me protected route — HTTP 401
[PASS] Accounting/Xero status protected route — HTTP 401

===== G) Whole-site manual test order =====
[CHECK] 1. Signup/login/logout — Create user, verify session, logout, login again.
[CHECK] 2. Setup/business basics — Business name, branding/settings, timezone/GST where relevant.
[CHECK] 3. Client flow — Add client, edit client, open detail modal/sheet, import client CSV.
[CHECK] 4. Team/worker flow — Add worker, confirm worker can see assigned work only.
[CHECK] 5. Job flow — Create job, assign worker, schedule it, edit it, open details.
[CHECK] 6. Worker proof flow — Acknowledge, start, pause, resume, add note/photo/proof, complete.
[CHECK] 7. Quote flow — Create quote, review it, convert or continue to job.
[CHECK] 8. Invoice flow — Completed job becomes invoice-ready, draft invoice review works.
[CHECK] 9. Command flow — Check for work, review proof, approve, needs edit, park for now.
[CHECK] 10. Money/admin debt — Unpaid/overdue/payment status/admin debt makes sense.
[CHECK] 11. Accounting sync — Xero page says draft sync only and owner-approved.
[CHECK] 12. Plans/pricing — Start/Crew/Operator/Command and add-on wording are clear.
[CHECK] 13. Imports/exports — CSV templates download and imports handle comma/semicolon/tab.
[CHECK] 14. Help/support — Owner can ask for help and support wording feels human.
[CHECK] 15. Mobile pass — Phone/tablet: buttons tappable, modals scroll, bottom nav does not block actions.
[CHECK] 16. Public/customer portal — Public quote/client links do not expose owner-only controls.

===== H) What still needs polish if tests feel rough =====
- If users feel lost: make Smart Hub simpler and put only Today, Money waiting, Approvals, and Setup gaps first.
- If workers struggle: strip worker view down to My Jobs, Acknowledge, Start/Pause, Complete, Proof.
- If Command feels busy: show fewer slips and make the approve/edit/park buttons bigger.
- If invoicing feels hidden: add a louder Ready to invoice badge on completed jobs.
- If pricing feels scary: make trial/no-card/no-surprise-sync wording clearer.
- If support feels cold: add a simple 'Need help setting this up?' card.

===== Summary =====
PASS: 43
WARN: 0
CHECK: 16
FAIL: 0

Result: READY FOR WHOLE-SITE MANUAL TEST PASS.
