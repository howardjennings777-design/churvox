Churvox timer/complete-job launch audit
======================================

--- backend/server.py ---
48: completed = job.get("completed") is True
49: completed_at = bool(job.get("completed_at"))
52: status == "completed" or
53: job_status == "completed" or
54: workflow_status == "completed" or
55: completed or
56: completed_at
58: job["status"] = "completed"
59: job["job_status"] = "completed"
60: job["workflow_status"] = "completed"
61: job["completed"] = True
542: ACKNOWLEDGED = "acknowledged"
545: COMPLETED = "completed"
1286: return {"success": False, "detail": "Please complete your account setup using the invite link sent to your email."}
1692: "status": JobStatus.COMPLETED,
1699: "completed_job_count": {"$sum": 1},
1706: jobs_by_worker[row.get("_id")] = int(row.get("completed_job_count") or 0)
1716: worker["completed_job_count"] = int(jobs_by_worker.get(worker_id, 0) or 0)
2179: "acknowledged_at": None,
2181: "completed_at": None,
2185: "timer_running": False,
2242: end_of_day = start_of_day + timedelta(days=1)
2331: @api_router.post("/jobs/{job_id}/acknowledge")
2332: async def acknowledge_job(job_id: str, request: Request, current_user: dict = Depends(get_current_user)):
2336: raise HTTPException(status_code=403, detail="Only workers can acknowledge jobs")
2343: {"$set": {"status": JobStatus.ACKNOWLEDGED, "acknowledged_at": datetime.now(timezone.utc)}}
2353: @api_router.post("/jobs/{job_id}/start")
2360: "status": {"$in": [JobStatus.ASSIGNED, JobStatus.ACKNOWLEDGED]}
2367: {"$set": {"status": JobStatus.IN_PROGRESS, "started_at": datetime.now(timezone.utc)}}
2407: async def create_draft_invoice_for_completed_job(job: dict, completed_total_seconds: int = 0):
2408: """Create one owner-review draft invoice when a job is completed. Never sends it."""
2436: seconds_worked = int(completed_total_seconds or job.get("total_time_seconds") or 0)
2463: or str(job.get("job_type") or "Completed job").replace("_", " ").title()
2493: "notes": "Auto-created as draft when worker completed the job. Owner must review before sending.",
2505: "source": "completed_job_auto_draft",
2527: @api_router.post("/jobs/{job_id}/complete")
2528: async def complete_job(job_id: str, request: Request, user = Depends(get_current_user)):
2543: if job.get("timer_running"):
2544: final_entry = {"action": "pause", "timestamp": now}
2552: "status": JobStatus.COMPLETED,
2553: "completed": True,
2554: "completed_at": now,
2555: "timer_running": False,
2556: "timer_started_at": None,
2568: invoice = await create_draft_invoice_for_completed_job(updated_job, new_total)
2571: logger.warning(f"Auto draft invoice failed for completed job {job_id}: {invoice_error}")
2575: "message": "Job completed successfully",
2577: "status": "completed",
2578: "completed": True,
2579: "timer_running": False,
2581: "completed_at": now.isoformat(),
2593: raise HTTPException(status_code=500, detail=f"Complete job failed: {str(e)}")
2626: total += (datetime.now(timezone.utc) - last_start).total_seconds()
2629: @api_router.post("/jobs/{job_id}/timer/start")
2632: @api_router.post("/jobs/{job_id}/time/start")
2635: @api_router.post("/jobs/{job_id}/start-timer")
2638: @api_router.post("/jobs/{job_id}/timer/begin")
2641: @api_router.post("/time-tracking/{job_id}/start")
2642: async def timer_start(job_id: str, request: Request, current_user: dict = Depends(get_current_user)):
2651: if job.get("timer_running"):
2652: raise HTTPException(status_code=400, detail="Timer already running")
2655: entry = {"action": "start", "timestamp": now}
2656: updates = {"$push": {"time_entries": entry}, "$set": {"timer_running": True, "timer_started_at": now}}
2657: if job["status"] in (JobStatus.ASSIGNED, JobStatus.ACKNOWLEDGED):
2666: @api_router.post("/jobs/{job_id}/timer/pause")
2668: @api_router.post("/jobs/{job_id}/time/pause")
2670: @api_router.post("/jobs/{job_id}/pause-timer")
2672: @api_router.post("/time-tracking/{job_id}/pause")
2673: async def timer_pause(job_id: str, request: Request, current_user: dict = Depends(get_current_user)):
2682: if not job.get("timer_running"):
2683: raise HTTPException(status_code=400, detail="Timer not running")
2686: entry = {"action": "pause", "timestamp": now}
2690: "$set": {"timer_running": False, "timer_started_at": None, "total_time_seconds": elapsed, "status": JobStatus.PAUSED}
2697: @api_router.post("/jobs/{job_id}/timer/resume")
2699: @api_router.post("/jobs/{job_id}/time/resume")
2701: @api_router.post("/jobs/{job_id}/resume-timer")
2703: @api_router.post("/time-tracking/{job_id}/resume")
2704: async def timer_resume(job_id: str, request: Request, current_user: dict = Depends(get_current_user)):
2713: if job.get("timer_running"):
2714: raise HTTPException(status_code=400, detail="Timer already running")
... 79 more matches

--- backend/churvox_job_timer_routes_patch.py ---
12: TIMER_PATHS = {
14: "/api/jobs/{job_id}/timer/start",
15: "/api/jobs/{job_id}/time/start",
16: "/api/jobs/{job_id}/start-timer",
20: "/api/jobs/{job_id}/timer/pause",
21: "/api/jobs/{job_id}/time/pause",
22: "/api/jobs/{job_id}/pause-timer",
26: "/api/jobs/{job_id}/timer/resume",
27: "/api/jobs/{job_id}/time/resume",
28: "/api/jobs/{job_id}/resume-timer",
31: "complete": [
32: "/api/jobs/{job_id}/timer/complete",
33: "/api/jobs/{job_id}/time/complete",
34: "/api/jobs/{job_id}/complete-timer",
35: "/api/jobs/{job_id}/complete",
122: raw = job.get("timer_started_at") or job.get("current_timer_started_at") or job.get("started_at")
139: started = started.replace(tzinfo=timezone.utc)
145: async def apply_timer_action(db, job, action, user, payload):
148: add_seconds = elapsed_seconds(job, at) if action in {"pause", "complete"} else 0
153: set_fields = {"updated_at": at, "timer_last_action": action, "last_timer_event": event}
160: "timer_status": "running",
161: "timer_running": True,
162: "timer_started_at": at,
163: "current_timer_started_at": at,
170: "timer_status": "running",
171: "timer_running": True,
172: "timer_started_at": at,
173: "current_timer_started_at": at,
179: "timer_status": "paused",
180: "timer_running": False,
186: unset_fields.update({"timer_started_at": "", "current_timer_started_at": ""})
187: elif action == "complete":
189: "status": "Completed",
190: "job_status": "Completed",
191: "timer_status": "stopped",
192: "timer_running": False,
196: "completed": True,
197: "completed_at": at,
198: "completed_by": actor,
201: unset_fields.update({"timer_started_at": "", "current_timer_started_at": ""})
203: update = {"$set": set_fields, "$push": {"time_events": event, "timer_events": event}}
209: await db.time_logs.insert_one({
238: async def handle_timer_action(request: Request, job_id: str, action: str):
244: updated = await apply_timer_action(db, job, action, user, payload)
245: return {"success": True, "action": action, "job": safe_job(updated), "status": updated.get("status"), "timer_status": updated.get("timer_status"), "time_seconds": updated.get("time_seconds") or updated.get("total_time_se
247: for action, paths in TIMER_PATHS.items():
251: return await handle_timer_action(request, job_id, _action)
252: endpoint.__name__ = f"churvox_{action}_timer_endpoint"

--- frontend/src/churvox-fresh/FreshJobs.jsx ---
7: const filters = ["All", "Ready", "In progress", "Blocked", "Completed", "Needs invoice"];
9: const TIMER_OPTIMISTIC = {
10: start: { status: "In progress", job_status: "In Progress", timer_status: "running", timer_running: true },
11: pause: { status: "In progress", job_status: "In Progress", timer_status: "paused", timer_running: false },
12: resume: { status: "In progress", job_status: "In Progress", timer_status: "running", timer_running: true },
13: complete: { status: "Completed", job_status: "Completed", timer_status: "stopped", timer_running: false, completed: true, invoice_ready: true },
40: function statusLabel(value) { const text = lower(value).replace(/[_-]+/g, " "); if (/complete|done|finished/.test(text)) return "Completed"; if (/progress|started|active/.test(text)) return "In progress"; if (/block|hold
62: function needsInvoice(job, invoices) { return Boolean(job?.status === "Completed" && !hasLinkedInvoice(job, invoices)); }
63: function storyStepState({ selected, quotes, invoices }) { const hasQuote = quotes.length > 0; const hasInvoice = hasJobInvoiceMarker(selected) || invoices.length > 0; const paid = invoices.some((invoice) => /paid|complet
65: function jobFromTimerResponse(data) { return data?.job || data?.data?.job || data?.data || data || null; }
132: const selectedCanArchive = Boolean(selected?.status === "Completed" && !selectedNeedsInvoice && !isRecurringJob(selected));
172: function archiveSelectedJob() { if (!selectedCanArchive || !selected) return; const nextIds = [...new Set([...archivedJobIds, String(selected.id)])]; setArchivedJobIds(nextIds); setActionMessage("Completed one-off job ar
194: const optimistic = TIMER_OPTIMISTIC[action] || {}; const previous = selected;
196: const res = await post(`/jobs/${selected.id}/timer/${action}`, { source: "fresh_jobs", action }, { timeout: 25000 });
198: const updated = jobFromTimerResponse(res.data); if (updated) patchLocalJob(selected.id, updated);
199: setActionMessage(action === "complete" ? "Job completed. Invoice can now be prepared." : `Timer ${action} saved.`); if (action === "complete") loadStory(); setActionBusy("");
213: const timerDisabled = !selected || Boolean(actionBusy);
216: <section className="freshJobsPage" data-jobs-needs-invoice="20260626" data-owner-timer-actions="20260626" data-create-job-modal="20260626" data-client-job-handoff="20260626" data-one-off-archive-prompt="20260626">
218: <section className="freshCommandPulse"><aside className="freshCard"><h2>{activeJobs.length}</h2><p>Active jobs</p></aside><aside className="freshCard"><h2>{activeJobs.filter((job) => job.status === "Ready").length}</h2><
223: <aside className="freshCard freshJobsListCard"><h2>Job list</h2>{loading && jobs.length === 0 ? <div className="freshItem"><b>Loading jobs...</b><span>Checking saved job records.</span></div> : visibleJobs.map((job) => {
226: <div className="freshJobsDetailHeader"><div><small>Job record</small><h2>{selected?.title || "Select job"}</h2></div>{selected ? <span className={selectedNeedsInvoice ? "need" : selected.status === "Completed" ? "ready" 
228: {selectedNeedsInvoice ? <section className="freshJobsInvoiceAlert"><strong>Completed job needs invoice</strong><p>This job is finished and no linked invoice was found. Create the invoice so Command can prepare the approv
229: {selected.status === "Completed" && !isRecurringJob(selected) ? <section className="freshJobsInvoiceAlert"><strong>One-off job finished</strong><p>{selectedNeedsInvoice ? "Create the invoice first. After it is invoiced, 
230: <div className="freshMiniGrid freshJobsMiniGrid"><div><span>Client</span><b>{selected.client}</b></div><div className={selectedNeedsInvoice ? "need" : ""}><span>Status</span><b>{selectedNeedsInvoice ? "Completed - needs 
231: <section className="freshActions freshJobsActionStack" style={{ marginTop: 12 }}><button className="freshPrimary" type="button" disabled={timerDisabled || selected.status === "In progress"} onClick={() => runJobAction("s

--- frontend/src/churvox-fresh/FreshWorkerCommand.jsx ---
112: function isComplete(job) {
113: return ["completed", "complete", "done", "finished"].includes(statusOf(job));
120: function isAcknowledged(job) {
121: return Boolean(pick(job, "acknowledged_at", "worker_acknowledged_at")) || ["acknowledged", "in_progress", "paused", "started", "completed"].includes(statusOf(job));
325: if (!isAcknowledged(job)) return "Acknowledge";
326: if (["assigned", "scheduled", "acknowledged"].includes(status)) return "Start job";
327: if (["in_progress", "started"].includes(status)) return "Pause / complete";
329: if (isComplete(job)) return "Completed";
336: if (isComplete(job) && hasPhotos && hasNotes) return "Photo + message sent";
337: if (isComplete(job) && hasPhotos) return "Photo proof added";
338: if (isComplete(job) && hasNotes) return "Worker message sent";
339: if (isComplete(job)) return "Complete — proof check";
368: return review === "ready_for_review" || review === "ready" || job?.completed_by_worker === true || isComplete(job);
446: const saved = seconds(job?.total_job_seconds || job?.total_time_seconds || job?.timer_total_seconds || job?.job_seconds || job?.total_seconds || job?.total_time_on_site_seconds || job?.time_seconds || job?.duration_secon
456: if (["pause", "stop", "complete", "finish"].includes(action) && start) {
457: total += Math.max(0, Math.floor((date.getTime() - start.getTime()) / 1000));
464: const timerStart = dateValue(job, "timer_started_at", "started_at");
465: if (!total && ["in_progress", "started"].includes(statusOf(job)) && timerStart) total = secondsSince(timerStart);
471: return pick(job, "completed_at", "timer_completed_at", "timer_paused_at", "timer_started_at", "started_at", "acknowledged_at", "worker_acknowledged_at", "updated_at", "modified_at");
526: const todayJobs = assignedJobs.filter((job) => isToday(dateValue(job, "scheduled_date", "date", "start", "start_time", "due_date")));
527: const currentJob = todayJobs.find(isActive) || assignedJobs.find(isActive) || todayJobs.find((job) => !isComplete(job)) || null;
530: const shiftStart = dateValue(worker, "shift_started_at", "clock_in_time", "shift_start_time", "last_clock_in_at");
533: const completedToday = todayJobs.filter(isComplete).length;
534: const remainingToday = todayJobs.filter((job) => !isComplete(job)).length;
541: if (todayJobs.some((job) => !isComplete(job) && !pick(job, "acknowledged_at", "worker_acknowledged_at"))) alerts.push("One or more jobs still need acknowledgement.");
542: if (todayJobs.some((job) => isComplete(job) && photosForJob(job).length === 0)) alerts.push("Finished jobs may need completion photos.");
544: return { assignedJobs, todayJobs, currentJob, currentJobSeconds, jobTimeSeconds, shiftSeconds, unallocatedSeconds, completedToday, remainingToday, alerts };
548: const when = dateValue(job, "scheduled_date", "date", "start", "start_time", "due_date");
605: : view.assignedJobs.filter((job) => !isComplete(job))).slice(0, 5)
607: const recentCompleted = view ? view.assignedJobs.filter(isComplete).slice(0, 5) : [];
674: const timer = window.setInterval(refreshLiveWorkerView, 8000);
677: window.clearInterval(timer);
791: <small>{current.title ? `On: ${current.title}` : `${itemView.remainingToday} left · ${itemView.completedToday} done`}</small>
889: ) : <div className="cvWorkerEmpty"><b>No photos uploaded yet</b><span>Completed job photos will show here.</span></div>}
932: <h3>Recently completed</h3>
934: {recentCompleted.length ? recentCompleted.map((job, index) => (
936: )) : <div className="cvWorkerEmpty"><b>No completed jobs yet</b><span>Completed work will show here.</span></div>}
998: <em>{dayText(dateValue(panelModal.job, "scheduled_date", "date", "start", "start_time", "due_date"))}</em>

--- frontend/src/components/forms/JobCreateForm.jsx ---
No obvious timer/status matches found.

--- frontend/src/hooks/useApi.js ---
No obvious timer/status matches found.

--- Summary ---
Needs review:
- server.py may not mount/import churvox_job_timer_routes_patch
