import React, { useCallback, useEffect, useMemo, useState } from "react";
import Layout from "../components/Layout";
import { ChurvoxLogo } from "../components/ChurvoxLogo";
import { get, post, patch } from "../lib/api";
import "../styles/aiControlRoom.css";

const STAGED_KEY = "churvox_control_room_staged";
const ADMIN = ["plans", "account", "settings", "contact", "notifications", "integrations", "privacy", "terms", "account_removal"];
const norm = (v) => String(v || "").toLowerCase();
const idOf = (x) => String(x?.id || x?._id || x?.uuid || "");
const titleOf = (x, f = "Item") => x?.title || x?.name || x?.business_name || x?.company_name || x?.invoice_number || x?.quote_number || x?.email || f;
const money = (v) => `$${Number(v || 0).toFixed(2)}`;
const arr = (v) => Array.isArray(v) ? v : Array.isArray(v?.data) ? v.data : Array.isArray(v?.items) ? v.items : Array.isArray(v?.actions) ? v.actions : Array.isArray(v?.jobs) ? v.jobs : Array.isArray(v?.clients) ? v.clients : Array.isArray(v?.invoices) ? v.invoices : Array.isArray(v?.quotes) ? v.quotes : Array.isArray(v?.workers) ? v.workers : Array.isArray(v?.proof_packs) ? v.proof_packs : [];

const approvals = [["all", "All approvals", "Everything AI prepared"], ["dispatch", "Dispatch", "Assign crew and keep jobs moving"], ["revenue", "Revenue", "Pricing and draft invoices"], ["follow", "Follow-ups", "Invoice and quote reminders"], ["proof", "Proof", "Proof packs before payment"], ["reception", "Receptionist", "New enquiries to triage"], ["recurring", "Recurring", "Repeat work due next"], ["customer", "Customer updates", "Draft messages to approve"], ["quote_builder", "Quote builder", "Prepared quote drafts"], ["memory", "Client memory", "Notes and next actions"]];
const workspaces = [["jobs", "Jobs", "Create, edit, assign, price"], ["clients", "Clients", "Customers, properties, notes"], ["quotes", "Quotes", "Draft, follow up, convert"], ["invoices", "Invoices", "Drafts, reminders, payment chase"], ["team", "Team", "Workers, roles, workload"], ["dispatch", "Dispatch", "AI crew recommendations"], ["proof", "Proof-to-Paid", "Proof, updates, payment flow"], ["reception", "Receptionist", "Enquiries into work drafts"], ["recurring", "Recurring", "Scheduled repeat work"], ["customer", "Customer Updates", "Approval-first messages"], ["quote_builder", "Quote Builder", "Build quotes with AI"], ["memory", "Client Memory", "History and property notes"], ["plans", "Plans / Billing", "Plan, limits, usage"], ["account", "Account Centre", "Owner account health"], ["settings", "Settings", "Business setup"], ["contact", "Contact Us", "Support request draft"], ["notifications", "Notifications", "Owner alert controls"], ["integrations", "Integrations", "MYOB, SMS, payments"], ["privacy", "Privacy", "Policy"], ["terms", "Terms", "Terms of use"], ["account_removal", "Account Removal", "Owner-only account request"]];

function loadStaged() { try { return JSON.parse(localStorage.getItem(STAGED_KEY) || "[]"); } catch { return []; } }
function saveStaged(items) { localStorage.setItem(STAGED_KEY, JSON.stringify(items.slice(0, 80))); }
function stageAction(action) { const item = { id: `staged-${Date.now()}-${Math.random().toString(36).slice(2)}`, type: "staged", priority: "medium", title: "Prepared action", summary: "Prepared safely in Control Room.", reason: "Owner approval first.", ...action, staged: true, created_at: new Date().toISOString() }; saveStaged([item, ...loadStaged()]); return item; }
function removeStaged(id) { if (id) saveStaged(loadStaged().filter((x) => x.id !== id)); }
function collection(data, key) { return { jobs: data.jobs, clients: data.clients, quotes: data.quotes, invoices: data.invoices, team: data.workers, dispatch: data.jobs, proof: data.proof, reception: data.reception, recurring: data.recurring, customer: data.customer, quote_builder: data.quoteDrafts, memory: data.memory }[key] || []; }
function findJob(data, id) { return data.jobs.find((j) => idOf(j) === String(id)); }
function findClient(data, id) { return data.clients.find((c) => idOf(c) === String(id)); }
async function tryPost(paths, payload) { let err; for (const p of paths) { try { return await post(p, payload); } catch (e) { err = e; } } throw err; }
async function tryPatch(paths, payload) { let err; for (const p of paths) { try { return await patch(p, payload); } catch (e) { err = e; } } throw err; }

function invoiceFromJob(job, data) {
  const client = findClient(data, job?.client_id || job?.customer_id) || {};
  const amount = Number(job?.price || job?.fixed_price || job?.amount || 0);
  const customer = job?.customer_name || job?.client_name || client?.name || client?.business_name || titleOf(job, "Customer");
  return { job_id: idOf(job), client_id: job?.client_id || job?.customer_id || idOf(client), customer_name: customer, customer_email: job?.customer_email || client?.email || "", address: job?.address || job?.job_address || client?.address || "", description: job?.ai_invoice_description || job?.invoice_description_draft || job?.description || job?.notes || `Service work completed for ${customer}.`, subtotal: amount, gst_rate: 15, notes: "Draft prepared from AI Control Room." };
}

function makeActions(data) {
  const out = [];
  const invoiced = new Set(data.invoices.map((i) => String(i.job_id || i.source_job_id || "")));
  const proofed = new Set(data.proof.map((p) => String(p.job_id || "")));
  data.jobs.forEach((j) => {
    const jid = idOf(j); const status = norm(j.status); const closed = ["completed", "complete", "closed", "cancelled", "canceled"].includes(status);
    if (!closed && !j.worker_id && !j.assigned_worker_id) out.push({ id: `dispatch-${jid}`, type: "dispatch", priority: "high", title: `Assign worker for ${titleOf(j, "job")}`, summary: "Job has no worker assigned.", reason: "Unassigned work blocks the day.", job_id: jid });
    if (["completed", "complete"].includes(status) && !invoiced.has(jid)) out.push({ id: `invoice-${jid}`, type: Number(j.price || j.fixed_price || 0) > 0 ? "revenue" : "pricing", priority: "high", title: Number(j.price || j.fixed_price || 0) > 0 ? `Create draft invoice for ${titleOf(j, "job")}` : `Add price for ${titleOf(j, "job")}`, summary: "Completed job is not invoiced yet.", reason: "Revenue is waiting.", job_id: jid });
    if (["completed", "complete"].includes(status) && !proofed.has(jid)) out.push({ id: `proof-${jid}`, type: "proof", priority: "medium", title: `Prepare proof pack for ${titleOf(j, "job")}`, summary: "Completed job has no proof pack.", reason: "Proof supports payment and customer updates.", job_id: jid });
  });
  data.invoices.filter((i) => ["open", "sent", "overdue", "unpaid", "pending_payment"].includes(norm(i.status))).forEach((i) => out.push({ id: `invoice-follow-${idOf(i)}`, type: "follow", priority: "medium", title: `Prepare invoice reminder ${i.invoice_number || idOf(i).slice(-6)}`, summary: `${money(i.balance_due || i.balance || i.total || i.amount)} waiting.`, reason: "Money is waiting.", invoice_id: idOf(i), client_id: i.client_id }));
  data.quotes.filter((q) => ["sent", "pending", "waiting", "viewed", "draft"].includes(norm(q.status))).forEach((q) => out.push({ id: `quote-follow-${idOf(q)}`, type: "follow", priority: "medium", title: `Prepare quote follow-up ${q.quote_number || idOf(q).slice(-6)}`, summary: "Quote needs a decision.", reason: "Follow-up can win work.", quote_id: idOf(q), client_id: q.client_id }));
  return out;
}

export default function AIControlRoomWiredPage() {
  const [data, setData] = useState({ jobs: [], clients: [], invoices: [], quotes: [], workers: [], proof: [], reception: [], recurring: [], customer: [], quoteDrafts: [], memory: [], billing: {}, user: {}, business: {}, notifications: {}, myob: {}, sms: {} });
  const [actions, setActions] = useState([]); const [drawer, setDrawer] = useState(null); const [selectedJob, setSelectedJob] = useState(null); const [jobDraft, setJobDraft] = useState({}); const [askText, setAskText] = useState("What should I do first?"); const [askAnswer, setAskAnswer] = useState(""); const [notice, setNotice] = useState(""); const [fallbackMode, setFallbackMode] = useState(false);
  const load = useCallback(async () => {
    const paths = ["/command-hub/actions", "/jobs", "/clients", "/invoices", "/quotes", "/team/workers", "/proof-packs", "/ai/receptionist/enquiries", "/ai/recurring", "/ai/customer-updates", "/ai/quotes/drafts", "/ai/client-memory", "/billing/status", "/auth/me", "/settings/business", "/notifications/settings", "/myob/status", "/sms/balance"];
    const r = await Promise.all(paths.map((p) => get(p).catch(() => null)));
    const next = { jobs: arr(r[1]), clients: arr(r[2]), invoices: arr(r[3]), quotes: arr(r[4]), workers: arr(r[5]), proof: arr(r[6]), reception: arr(r[7]), recurring: arr(r[8]), customer: arr(r[9]), quoteDrafts: arr(r[10]), memory: arr(r[11]), billing: r[12]?.data || r[12] || {}, user: r[13]?.data || r[13] || {}, business: r[14]?.data || r[14] || {}, notifications: r[15]?.data || r[15] || {}, myob: r[16]?.data || r[16] || {}, sms: r[17]?.data || r[17] || {} };
    const backend = arr(r[0]); setFallbackMode(!backend.length); setActions([...loadStaged(), ...(backend.length ? backend : makeActions(next))]); setData(next);
  }, []);
  useEffect(() => { load(); }, [load]);
  const moneyWaiting = useMemo(() => data.invoices.filter((i) => ["open", "sent", "overdue", "unpaid", "pending_payment"].includes(norm(i.status))).reduce((s, i) => s + Number(i.balance_due || i.balance || i.total || i.amount || 0), 0), [data.invoices]);
  const counts = useMemo(() => ({ dispatch: actions.filter((a) => norm(a.type).includes("dispatch")).length, revenue: actions.filter((a) => ["revenue", "invoice", "pricing"].some((x) => norm(a.type).includes(x))).length, follow: actions.filter((a) => norm(a.type).includes("follow")).length, proof: actions.filter((a) => norm(a.type).includes("proof")).length, reception: actions.filter((a) => norm(a.type).includes("reception")).length, recurring: actions.filter((a) => norm(a.type).includes("recurring")).length, customer: actions.filter((a) => norm(a.type).includes("customer")).length, quote_builder: actions.filter((a) => norm(a.type).includes("quote")).length, memory: actions.filter((a) => norm(a.type).includes("memory")).length }), [actions]);
  const activeJobs = useMemo(() => data.jobs.filter((j) => !["completed", "complete", "closed", "cancelled", "canceled"].includes(norm(j.status))).slice(0, 8), [data.jobs]); const best = actions[0]?.title || "Review today’s work plan";
  const openJob = (jobOrId) => { const job = typeof jobOrId === "string" ? findJob(data, jobOrId) : (findJob(data, idOf(jobOrId)) || jobOrId); if (!job) return; setSelectedJob(job); setJobDraft({ title: job.title || job.name || "", status: job.status || "assigned", worker_id: job.worker_id || job.assigned_worker_id || "", price: job.price || job.fixed_price || job.amount || 0, notes: job.notes || "" }); setDrawer({ kind: "job", title: titleOf(job, "Job"), desc: "Edit job, assign worker, invoice, proof." }); };
  const runScan = async () => { try { await tryPost(["/smart-hub/scan", "/ai/operator/run-daily-check", "/ai/operator/prepare-today"], {}); setNotice("AI scan completed."); } catch { setNotice("AI scan backend is not live yet. Local queue refreshed."); } await load(); };
  const saveJob = async () => { if (!selectedJob) return; const jobId = idOf(selectedJob); await patch(`/jobs/${jobId}`, { title: jobDraft.title, status: jobDraft.status, price: Number(jobDraft.price || 0), fixed_price: Number(jobDraft.price || 0), notes: jobDraft.notes || "" }); if (jobDraft.worker_id && jobDraft.worker_id !== (selectedJob.worker_id || selectedJob.assigned_worker_id)) { try { await tryPost([`/jobs/${jobId}/assign`, `/jobs/${jobId}/assign-worker`], { worker_id: jobDraft.worker_id }); } catch { stageAction({ type: "dispatch", title: `Assign worker for ${jobDraft.title || titleOf(selectedJob, "job")}`, summary: "Assignment staged because assign route did not accept it yet.", job_id: jobId, worker_id: jobDraft.worker_id }); } } setNotice("Job saved. Assignment applied or staged."); await load(); };
  const approve = async (action) => { const type = norm(action.type || action.action_type); const jobId = String(action.job_id || action.payload?.job_id || ""); const workerId = String(action.worker_id || action.payload?.worker_id || ""); try { if (type.includes("dispatch")) { if (!jobId || !workerId) { stageAction({ ...action, type: "dispatch", title: action.title || "Pick worker for assignment", summary: "Open job drawer and choose a worker first." }); setNotice("Assignment staged. Pick a worker first."); await load(); return; } await tryPost([`/jobs/${jobId}/assign`, `/jobs/${jobId}/assign-worker`], { worker_id: workerId }); removeStaged(action.id); setNotice("Worker assigned."); await load(); return; } if (type.includes("revenue") || type.includes("invoice")) { const job = findJob(data, jobId) || selectedJob || action.payload; const payload = invoiceFromJob(job || {}, data); if (!Number(payload.subtotal || 0)) { stageAction({ ...action, type: "pricing", title: action.title || "Add price before invoice", summary: "Add price in job drawer before invoice." }); setNotice("Price needed. Action staged."); await load(); return; } await post("/invoices", payload); removeStaged(action.id); setNotice("Draft invoice created."); await load(); return; } if (type.includes("proof")) { try { await post("/proof-packs", { job_id: jobId, title: action.title || "Proof pack", status: "draft", notes: "Prepared from AI Control Room" }); removeStaged(action.id); setNotice("Proof pack prepared."); } catch { stageAction({ ...action, type: "proof", summary: "Proof pack staged for review." }); setNotice("Proof staged for review."); } await load(); return; } if (ADMIN.some((x) => type.includes(x))) { stageAction(action); setNotice(`${action.title || "Owner action"} staged safely for approval.`); await load(); return; } try { await post("/command-hub/actions/execute", action); removeStaged(action.id); setNotice("Action completed."); } catch { stageAction({ ...action, summary: action.summary || "Prepared in Control Room staging." }); setNotice("Action staged safely."); } await load(); } catch (e) { setNotice(e?.message || "Action failed safely."); } };
  const askAi = async (prompt) => { const q = prompt || askText; try { const r = await tryPost(["/ai/operator/ask", "/ai/ask", "/smart-hub/ask"], { question: q, prompt: q }); setAskAnswer(r?.data?.answer || r?.data?.response || r?.answer || r?.response || "AI answered."); } catch { const a = actions[0]; setAskAnswer(a ? `Start with: ${a.title}. Why: ${a.reason || a.summary}. Next: ${a.next || "Open the drawer and approve."}` : "No urgent blocker found. Review active jobs then approvals."); } };

  return <Layout><main className="cr-room-page"><section className="cr-room-hero"><div><ChurvoxLogo size="hero" /><p className="cr-room-kicker">AI Operator</p><h1>AI Control Room</h1><p>AI prepares admin, dispatch, proof, invoices, follow-ups and customer work. Owner edits and approves inside drawers.</p><div className="cr-room-row"><button onClick={runScan}>Run AI scan</button><button onClick={() => setDrawer({ kind: "ask", title: "Ask AI", desc: "Ask the operator" })}>Ask AI</button><button onClick={() => setDrawer({ kind: "approvals", title: "Approvals", desc: "Review prepared work", filter: "all" })}>Open approvals</button></div></div><aside className="cr-room-score"><h3>Live score</h3><p>Approvals: {actions.length}</p><p>Workers active: {data.workers.length}</p><p>Money waiting: {money(moneyWaiting)}</p><p>Status: {fallbackMode ? "local AI fallback mode" : "live AI queue"}</p></aside></section><section className="cr-room-safety">Auto-send is OFF. AI prepares drafts for review. · No auto-charge · No MYOB write · No payroll changes · No deletion without owner approval.</section><section className="cr-room-zone cr-room-plan"><p className="cr-room-zone-label">Zone 1 · AI Today Plan</p><h2>{best}</h2><p>{actions[0]?.reason || "AI is using live business data and safe local rules."}</p><div className="cr-room-grid4"><Metric label="Need crew" value={counts.dispatch} /><Metric label="Revenue" value={`$${moneyWaiting.toFixed(0)}`} /><Metric label="Follow-ups" value={counts.follow} /><Metric label="Proof" value={counts.proof} /></div><div className="cr-room-row"><button onClick={() => setDrawer({ kind: "approvals", title: "Work plan", desc: "Prepared actions", filter: "all" })}>Work plan</button><button onClick={() => { setDrawer({ kind: "ask", title: "Explain plan", desc: "AI reasoning" }); askAi("Explain today’s plan"); }}>Explain plan</button></div></section><section className="cr-room-zone"><p className="cr-room-zone-label">Zone 2 · Next Best Moves</p><h2>Choose the next business move</h2><div className="cr-room-grid3"><MoveCard title="Dispatch the day" value={counts.dispatch} text="Assign workers and unblock today’s jobs" onClick={() => setDrawer({ kind: "workspace", key: "dispatch", title: "Dispatch", desc: "Assign workers" })} /><MoveCard title="Move money" value={counts.revenue + counts.follow} text="Draft invoices and follow-ups" onClick={() => setDrawer({ kind: "workspace", key: "invoices", title: "Invoices", desc: "Move money" })} /><MoveCard title="Proof & updates" value={counts.proof + counts.customer} text="Prepare proof packs and customer updates" onClick={() => setDrawer({ kind: "workspace", key: "proof", title: "Proof-to-Paid", desc: "Proof and updates" })} /></div></section><section className="cr-room-zone"><p className="cr-room-zone-label">Zone 3 · Active Work Board</p><h2>Jobs moving now</h2><div className="cr-room-list">{activeJobs.map((j) => <div className="cr-room-list-row" key={idOf(j)}><div><strong>{titleOf(j, "Job")}</strong><p>{j.client_name || "Client"} · {j.status || "assigned"} · {j.worker_name || j.assigned_worker_name || "Unassigned"}</p></div><button onClick={() => openJob(j)}>Work here</button></div>)}</div></section><section className="cr-room-zone"><p className="cr-room-zone-label">Zone 4 · AI Approval Control</p><h2>Approve what AI prepared</h2><div className="cr-room-grid5">{approvals.map(([key, label, desc]) => <button className="cr-room-approval" key={key} onClick={() => setDrawer({ kind: "approvals", title: label, desc, filter: key })}><strong>{label}</strong><span>{desc}</span><em>{key === "all" ? actions.length : countFor(counts, key)} ready</em></button>)}</div></section><section className="cr-room-zone"><p className="cr-room-zone-label">Zone 5 · Owner Workspaces</p><h2>Run every area from one hub</h2><div className="cr-room-grid5">{workspaces.map(([key, label, desc]) => <button className="cr-room-workspace" key={key} onClick={() => setDrawer({ kind: "workspace", key, title: label, desc })}><strong>{label}</strong><span>{desc}</span><em>{workspaceCount(data, key)}</em></button>)}</div></section>{notice ? <div className="cr-room-notice">{notice}</div> : null}{drawer ? <Drawer drawer={drawer} close={() => setDrawer(null)} data={data} actions={actions} counts={counts} askText={askText} setAskText={setAskText} askAnswer={askAnswer} askAi={askAi} jobDraft={jobDraft} setJobDraft={setJobDraft} selectedJob={selectedJob} openJob={openJob} saveJob={saveJob} approve={approve} /> : null}</main></Layout>;
}

function countFor(counts, key) { return key === "follow_ups" ? counts.follow : counts[key] || 0; }
function workspaceCount(data, key) { const items = collection(data, key); if (items.length) return `${items.length} loaded`; return ADMIN.includes(key) ? "ready" : "0 loaded"; }
function Metric({ label, value }) { return <div className="cr-room-metric"><span>{label}</span><strong>{value}</strong></div>; }
function MoveCard({ title, value, text, onClick }) { return <button className="cr-room-card" onClick={onClick}><small>Next move</small><h3>{title}</h3><b>{value}</b><p>{text}</p></button>; }
function Info({ title, text }) { return <div className="cr-room-drawer-card"><h4>{title}</h4><p>{text}</p></div>; }
function Check({ label, value, onChange }) { return <label className="cr-room-check"><input type="checkbox" checked={!!value} onChange={(e) => onChange(e.target.checked)} /> {label}</label>; }

function Drawer({ drawer, close, data, actions, counts, askText, setAskText, askAnswer, askAi, jobDraft, setJobDraft, selectedJob, openJob, saveJob, approve }) { const filtered = drawer.kind === "approvals" && drawer.filter && drawer.filter !== "all" ? actions.filter((a) => norm(a.type).includes(drawer.filter) || (drawer.filter === "revenue" && ["invoice", "pricing"].some((x) => norm(a.type).includes(x))) || (drawer.filter === "follow_ups" && norm(a.type).includes("follow"))) : actions; return <div className="cr-room-drawer-backdrop" onClick={close}><aside className="cr-room-drawer" onClick={(e) => e.stopPropagation()}><header><div><h3>{drawer.title}</h3><p>{drawer.desc}</p></div><button onClick={close}>Close</button></header><div className="cr-room-drawer-body">{drawer.kind === "ask" ? <AskDrawer askText={askText} setAskText={setAskText} askAnswer={askAnswer} askAi={askAi} /> : drawer.kind === "job" ? <JobDrawer data={data} selectedJob={selectedJob} jobDraft={jobDraft} setJobDraft={setJobDraft} saveJob={saveJob} approve={approve} /> : drawer.kind === "approvals" ? <ApprovalDrawer actions={filtered} approve={approve} openJob={openJob} /> : ADMIN.includes(drawer.key) ? <AdminDrawer drawer={drawer} data={data} askAi={askAi} approve={approve} /> : <WorkDrawer drawer={drawer} data={data} counts={counts} askAi={askAi} approve={approve} openJob={openJob} />}</div></aside></div>; }
function AskDrawer({ askText, setAskText, askAnswer, askAi }) { return <div className="cr-room-form"><label>Ask AI<textarea value={askText} onChange={(e) => setAskText(e.target.value)} /></label><div className="cr-room-row">{["What should I do first?", "Who should I assign first?", "What money is waiting?", "What proof packs are missing?"].map((p) => <button key={p} onClick={() => setAskText(p)}>{p}</button>)}</div><button onClick={() => askAi()}>Ask AI</button>{askAnswer ? <Info title="AI answer" text={askAnswer} /> : null}</div>; }
function JobDrawer({ data, selectedJob, jobDraft, setJobDraft, saveJob, approve }) { const jobId = idOf(selectedJob); return <div className="cr-room-form"><Info title={titleOf(selectedJob, "Job")} text="Edit the job, assign worker, create draft invoice, or prepare proof." />{["title", "status", "price", "notes"].map((k) => <label key={k}>{k}<input value={jobDraft[k] || ""} onChange={(e) => setJobDraft((s) => ({ ...s, [k]: e.target.value }))} /></label>)}<label>worker<select value={jobDraft.worker_id || ""} onChange={(e) => setJobDraft((s) => ({ ...s, worker_id: e.target.value }))}><option value="">Unassigned</option>{data.workers.map((w) => <option key={idOf(w)} value={idOf(w)}>{w.name || w.email}</option>)}</select></label><div className="cr-room-row"><button onClick={saveJob}>Save job</button><button onClick={() => approve({ type: "dispatch", job_id: jobId, worker_id: jobDraft.worker_id, title: `Assign worker for ${titleOf(selectedJob, "job")}` })}>Approve assignment</button><button onClick={() => approve({ type: "revenue", job_id: jobId, title: `Create invoice for ${titleOf(selectedJob, "job")}` })}>Create draft invoice</button><button onClick={() => approve({ type: "proof", job_id: jobId, title: `Prepare proof for ${titleOf(selectedJob, "job")}` })}>Prepare proof pack</button></div></div>; }
function ApprovalDrawer({ actions, approve, openJob }) { return <div className="cr-room-card-list">{actions.length ? actions.map((a) => <article key={a.id} className="cr-room-action"><span className="cr-room-pill">{a.staged ? "staged" : a.priority || "review"}</span><h3>{a.title}</h3><p>{a.summary}</p><p><strong>Why:</strong> {a.reason}</p><div className="cr-room-row">{a.job_id ? <button onClick={() => openJob(a.job_id)}>Edit job first</button> : null}<button onClick={() => approve(a)}>Approve action</button></div></article>) : <Info title="No actions" text="No actions in this group yet." />}</div>; }
function WorkDrawer({ drawer, data, counts, askAi, approve, openJob }) { const [draft, setDraft] = useState({ title: "", amount: "", notes: "" }); const items = collection(data, drawer.key).slice(0, 8); return <div className="cr-room-form"><Info title={drawer.title} text={drawer.desc} /><div className="cr-room-mini-stats"><span>{items.length} records</span><span>{counts.dispatch} dispatch</span><span>{counts.follow} follow-ups</span></div>{items.length ? <div className="cr-room-list">{items.map((x, i) => <div key={idOf(x) || i} className="cr-room-list-row"><div><strong>{titleOf(x, `Item ${i + 1}`)}</strong><p>{x.status || x.email || x.phone || "ready"}</p></div>{["jobs", "dispatch"].includes(drawer.key) ? <button onClick={() => openJob(x)}>Work here</button> : <button onClick={() => approve({ type: drawer.key, title: `Stage ${drawer.title}`, payload: x })}>Stage</button>}</div>)}</div> : null}<label>Title / customer<input value={draft.title} onChange={(e) => setDraft((s) => ({ ...s, title: e.target.value }))} /></label><label>Amount / status<input value={draft.amount} onChange={(e) => setDraft((s) => ({ ...s, amount: e.target.value }))} /></label><label>Notes<textarea value={draft.notes} onChange={(e) => setDraft((s) => ({ ...s, notes: e.target.value }))} /></label><div className="cr-room-row"><button onClick={() => askAi(`Prepare ${drawer.title}: ${draft.title} ${draft.notes}`)}>Ask AI to prepare</button><button onClick={() => approve({ type: drawer.key, title: `${drawer.title} staged`, summary: draft.title || draft.notes || "Prepared from drawer", payload: draft })}>Stage for approval</button></div></div>; }

function AdminDrawer({ drawer, data, askAi, approve }) { const [form, setForm] = useState(() => adminInitial(drawer.key, data)); const set = (k, v) => setForm((s) => ({ ...s, [k]: v })); const stage = (title, summary, extra = {}) => approve({ type: drawer.key, title, summary, reason: "Owner/admin drawer action prepared inside Control Room.", payload: { ...form, ...extra } }); if (drawer.key === "settings") return <div className="cr-room-form"><Info title="Business setup" text="Business identity, trade defaults, GST, and document wording." /><label>Business name<input value={form.business_name} onChange={(e) => set("business_name", e.target.value)} /></label><label>Industry / trade<select value={form.industry} onChange={(e) => set("industry", e.target.value)}><option>Lawn Care</option><option>Landscaping</option><option>Cleaning</option><option>Handyman</option><option>Painting</option><option>Plumbing</option><option>Electrical</option><option>Pest Control</option><option>Other</option></select></label><label>GST rate %<input value={form.gst_rate} onChange={(e) => set("gst_rate", e.target.value)} /></label><label>Default invoice note<textarea value={form.invoice_note} onChange={(e) => set("invoice_note", e.target.value)} /></label><div className="cr-room-row"><button onClick={() => saveBusinessSettings(form, stage)}>Save business setup</button><button onClick={() => askAi(`Improve my Churvox business setup: ${JSON.stringify(form)}`)}>Ask AI setup help</button></div></div>; if (drawer.key === "contact") return <div className="cr-room-form"><Info title="Contact Churvox support" text="Prepare a support request without leaving the Control Room." /><label>Issue type<select value={form.issue_type} onChange={(e) => set("issue_type", e.target.value)}><option>Bug</option><option>Billing</option><option>MYOB</option><option>Feature request</option><option>Urgent help</option></select></label><label>Subject<input value={form.subject} onChange={(e) => set("subject", e.target.value)} /></label><label>Message<textarea value={form.message} onChange={(e) => set("message", e.target.value)} /></label><div className="cr-room-row"><button onClick={() => stage("Support request prepared", form.subject || form.message || "Support request draft")}>Prepare support request</button><button onClick={() => askAi(`Clean this support request up: ${form.subject} ${form.message}`)}>Ask AI to clean it up</button></div></div>; if (drawer.key === "account") return <div className="cr-room-form"><Info title="Owner account centre" text="Review account health, owner details, and notes." /><div className="cr-room-grid4"><Metric label="Plan" value={form.plan || "review"} /><Metric label="Clients" value={data.clients.length} /><Metric label="Workers" value={data.workers.length} /><Metric label="Money" value={money(form.money_waiting)} /></div><label>Owner name<input value={form.owner_name} onChange={(e) => set("owner_name", e.target.value)} /></label><label>Owner email<input value={form.owner_email} onChange={(e) => set("owner_email", e.target.value)} /></label><label>Account note<textarea value={form.account_note} onChange={(e) => set("account_note", e.target.value)} /></label><button onClick={() => stage("Account centre update staged", "Owner account details/note prepared for approval")}>Stage account update</button></div>; if (drawer.key === "plans") return <div className="cr-room-form"><Info title="Plans / billing" text="Review current plan and prepare a plan change." /><div className="cr-room-grid4"><Metric label="Current" value={form.plan || "unknown"} /><Metric label="Status" value={form.status || "review"} /><Metric label="Clients" value={data.clients.length} /><Metric label="Workers" value={data.workers.length} /></div><PlanButtons onChoose={(plan) => stage(`Plan change prepared: ${plan}`, `Owner wants to review ${plan} plan`, { requested_plan: plan })} /></div>; if (drawer.key === "notifications") return <div className="cr-room-form"><Info title="Owner alert controls" text="Choose owner alerts. No customer message is sent here." /><Check label="Job completed" value={form.job_completed} onChange={(v) => set("job_completed", v)} /><Check label="Invoice overdue" value={form.invoice_overdue} onChange={(v) => set("invoice_overdue", v)} /><Check label="Proof pack ready" value={form.proof_ready} onChange={(v) => set("proof_ready", v)} /><Check label="Worker note added" value={form.worker_note} onChange={(v) => set("worker_note", v)} /><button onClick={() => stage("Notification preferences staged", "Owner alert settings prepared", form)}>Stage notification settings</button></div>; if (drawer.key === "integrations") return <div className="cr-room-form"><Info title="Integrations" text="MYOB, SMS, and payment setup. MYOB writes stay approval-first." /><div className="cr-room-grid4"><Metric label="MYOB" value={form.myob_status} /><Metric label="SMS" value={String(form.sms_balance)} /><Metric label="Payments" value={form.payment_status} /><Metric label="Mode" value="approval" /></div><label>MYOB company file / note<input value={form.myob_note} onChange={(e) => set("myob_note", e.target.value)} /></label><label>Integration note<textarea value={form.integration_note} onChange={(e) => set("integration_note", e.target.value)} /></label><div className="cr-room-row"><button onClick={() => stage("MYOB setup review staged", "MYOB integration details prepared", form)}>Stage MYOB review</button><button onClick={() => stage("SMS/payment review staged", "SMS/payment integration review prepared", form)}>Stage SMS/payment review</button></div></div>; if (drawer.key === "privacy") return <PolicyDrawer title="Privacy" type="privacy" form={form} set={set} stage={stage} askAi={askAi} />; if (drawer.key === "terms") return <PolicyDrawer title="Terms" type="terms" form={form} set={set} stage={stage} askAi={askAi} />; if (drawer.key === "account_removal") return <div className="cr-room-form"><Info title="Account removal" text="Owner-only request. This prepares a request only. Nothing is deleted here." /><label>Reason<select value={form.reason} onChange={(e) => set("reason", e.target.value)}><option>Testing only</option><option>Business closed</option><option>Changing provider</option><option>Privacy request</option><option>Other</option></select></label><label>Details<textarea value={form.details} onChange={(e) => set("details", e.target.value)} /></label><Check label="I understand this only prepares a request, it does not delete anything now" value={form.ack} onChange={(v) => set("ack", v)} /><button onClick={() => stage("Account removal request prepared", "Owner-only account removal request staged", form)}>Prepare owner request</button></div>; return null; }
function adminInitial(key, data) { const billing = data.billing || {}; const user = data.user || {}; const business = data.business || {}; const sms = data.sms || {}; const myob = data.myob || {}; const waiting = data.invoices.filter((i) => ["open", "sent", "overdue", "unpaid", "pending_payment"].includes(norm(i.status))).reduce((s, i) => s + Number(i.balance_due || i.balance || i.total || i.amount || 0), 0); if (key === "settings") return { business_name: business.business_name || user.business_name || "", industry: business.industry || business.trade_type || "Lawn Care", gst_rate: business.gst_rate || 15, invoice_note: business.invoice_note || "Thank you for your business." }; if (key === "contact") return { issue_type: "Bug", subject: "", message: "" }; if (key === "account") return { owner_name: user.name || "", owner_email: user.email || "", plan: billing.plan || user.plan || "review", money_waiting: waiting, account_note: "" }; if (key === "plans") return { plan: billing.plan || user.plan || "unknown", status: billing.status || billing.plan_status || "review" }; if (key === "notifications") return { job_completed: true, invoice_overdue: true, proof_ready: true, worker_note: true, ...(data.notifications || {}) }; if (key === "integrations") return { myob_status: myob.connected ? "connected" : myob.status || "review", sms_balance: sms.balance ?? sms.credits ?? "review", payment_status: billing.stripe_subscription_id ? "active" : "review", myob_note: myob.company_file_name || "", integration_note: "" }; if (key === "privacy" || key === "terms") return { note: "", audience: "Owner review", change_type: "Review" }; if (key === "account_removal") return { reason: "Testing only", details: "", ack: false }; return {}; }
async function saveBusinessSettings(form, stage) { try { await tryPatch(["/settings/business", "/account/business", "/users/me"], form); stage("Business setup saved", "Business settings were sent to the backend", form); } catch { stage("Business setup staged", "Backend settings route was not ready, so the update is staged safely", form); } }
function PlanButtons({ onChoose }) { return <div className="cr-room-card-list">{["solo", "team", "pro", "enterprise"].map((p) => <article key={p} className="cr-room-action"><h3>{p[0].toUpperCase() + p.slice(1)}</h3><p>{p === "solo" ? "Simple owner plan" : p === "team" ? "Crew workflow" : p === "pro" ? "Advanced trade operations" : "Scale and MYOB included"}</p><button onClick={() => onChoose(p)}>Prepare {p} review</button></article>)}</div>; }
function PolicyDrawer({ title, type, form, set, stage, askAi }) { return <div className="cr-room-form"><Info title={title} text={`Review ${title.toLowerCase()} wording and stage owner notes without leaving the Control Room.`} /><label>Review type<select value={form.change_type} onChange={(e) => set("change_type", e.target.value)}><option>Review</option><option>Update wording</option><option>Customer question</option><option>Compliance note</option></select></label><label>Audience<input value={form.audience} onChange={(e) => set("audience", e.target.value)} /></label><label>Owner note<textarea value={form.note} onChange={(e) => set("note", e.target.value)} /></label><div className="cr-room-row"><button onClick={() => stage(`${title} review staged`, `${title} owner note prepared`, { policy_type: type })}>Stage {title} review</button><button onClick={() => askAi(`Help review this ${title}: ${form.note}`)}>Ask AI to review</button></div></div>; }
