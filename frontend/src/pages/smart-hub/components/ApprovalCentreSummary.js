import React from 'react';

export function ApprovalCentreSummary({ approvalCounts, priorityItems = [], bestNextMove, onOpen, onRunPlan }) {
  return <section className="mt-6 rounded-2xl border border-[#746c60] border-l-4 border-l-[#d94f17] bg-[#c8bfb1] p-4 shadow-[0_14px_35px_rgba(15,17,21,0.20)]"><div className="flex items-center justify-between rounded-xl bg-[#15181d] px-3 py-2 text-white"><h2 className="text-xs font-semibold uppercase tracking-[0.16em]">AI Approval Centre</h2><span className="text-xs text-white/70">{approvalCounts.all || 0} approvals</span></div><div className="mt-3 max-h-44 space-y-2 overflow-hidden">{!!priorityItems.length ? priorityItems.slice(0, 3).map((item) => <button key={item.id} type="button" onClick={() => onOpen(bestNextMove?.approvalTab || 'all')} className="w-full rounded-xl bg-[#111317] px-3 py-2 text-left text-white"><p className="text-sm font-medium">{item.meta?.title || item.title}</p>{item.meta?.subtitle ? <p className="text-xs text-white/65">{item.meta.subtitle}</p> : null}</button>) : <div className="rounded-xl bg-[#111317] px-3 py-2 text-sm text-white">No approvals waiting right now.</div>}</div><p className="mt-3 text-xs text-[#3f3931]">{approvalCounts.needs_decision || 0} need decision · {approvalCounts.ready || 0} ready · {approvalCounts.drafts || 0} drafts · {approvalCounts.watching || 0} watching</p><div className="mt-3 flex flex-wrap gap-2"><button type="button" onClick={() => onOpen('all')} className="rounded-xl bg-[#d94f17] px-3 py-2 text-sm font-semibold text-white">Open Approval Centre</button><button type="button" onClick={onRunPlan} className="rounded-xl bg-[#111317] px-3 py-2 text-sm font-semibold text-white">Run today's AI plan</button></div></section>;
}
