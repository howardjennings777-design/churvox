import React from "react";
import { Link } from "react-router-dom";
import { toast } from "sonner";
import { industrialAction, industrialChip, industrialContentLane, industrialGhost, industrialPageShell } from "../components/industrialCommandTheme";
import { businessSettingsCompletion, loadBusinessSettings, saveBusinessSettings } from "../lib/businessSettings";

const tileStyle = {
  background: "linear-gradient(135deg, #111827, #070d16)",
  color: "#ffffff",
  boxShadow: "0 18px 46px rgba(2,6,23,.26), inset 0 1px 0 rgba(255,255,255,.06)",
};

const sections = [
  { id: "profile", title: "Business profile", badge: "Real save", color: "#fb923c", href: "#business-settings", copy: "Business name, customer contact details, address, GST, invoice terms and quote terms are saved here.", details: [["Purpose", "Keep the business identity and customer-facing details correct."], ["Save path", "Local business settings helper used by quote and invoice forms."], ["Rule", "Only settings that affect live forms stay here."]] },
  { id: "branding", title: "Branding", badge: "Customer facing", color: "#22d3ee", href: "#business-settings", copy: "Logo and document defaults used by quotes and invoices.", details: [["Purpose", "Keep customer-facing quotes, invoices and screens consistent."], ["Check", "Logo, invoice prefix, quote prefix, wording and document presentation."], ["Rule", "No random old themes or mismatched branding."]] },
  { id: "accounting", title: "Xero accounting", badge: "Approval first", color: "#34d399", href: "/support-board", copy: "Xero is visible as staged accounting direction. Sync remains approval-first until fully certified and tested.", details: [["Purpose", "Prepare invoice/payment sync direction without overpromising."], ["Check", "Accounting wording, support path and approval-first sync copy."], ["Rule", "No automatic accounting changes without owner approval."]] },
  { id: "plans", title: "Plan and billing", badge: "Owner only", color: "#facc15", href: "/plans", copy: "Start, Crew, Operator, Command and Growth Pack access.", details: [["Purpose", "Keep pricing and plan access clear."], ["Check", "Current plan, upgrade path and Growth Pack wording."], ["Rule", "Plan changes stay owner-controlled."]] },
  { id: "team", title: "Team access", badge: "Role safe", color: "#a78bfa", href: "/team-board", copy: "Roles, invites, worker access and payroll permissions.", details: [["Purpose", "Keep staff access safe and simple."], ["Check", "Owner, Manager, Office Admin, Worker and Payroll roles."], ["Rule", "Payroll stays locked down. Workers do not see pricing or GPS evidence."]] },
  { id: "legal", title: "Legal and support", badge: "Live links", color: "#f43f5e", href: "/support-board", copy: "Privacy, terms, account deletion, help and setup support.", details: [["Purpose", "Make support and legal pages easy to find."], ["Check", "Privacy, terms, account deletion and help links."], ["Rule", "Legal/support links must be tappable on mobile."]] },
];

const checklist = [
  "Business profile completed",
  "Logo and customer-facing details checked",
  "First client created",
  "First job created",
  "First invoice or quote reviewed",
  "Worker invite tested",
  "Xero shown as staged accounting direction",
  "Old experimental pages hidden from launch",
];

const industries = ["Lawn Care", "Landscaping", "Cleaning", "Handyman", "Painting", "Plumbing", "Electrical", "Pest Control", "Gardening", "Other"];

function Tape({ color = "#fb923c" }) { return <span aria-hidden="true" className="absolute left-0 top-0 h-full w-2.5 rounded-l-[30px]" style={{ background: `linear-gradient(180deg, ${color}, #facc15)`, boxShadow: `0 0 20px ${color}66` }} />; }
function DarkCard({ children, color = "#fb923c", className = "" }) { return <article className={`relative overflow-hidden rounded-[30px] border border-white/10 p-5 pl-8 text-white ${className}`} style={tileStyle}><Tape color={color} />{children}</article>; }
function Detail({ label, value }) { return <div className="rounded-2xl border border-white/10 bg-slate-950/45 p-4"><div className="text-[10px] font-black uppercase tracking-[.16em] text-amber-300">{label}</div><div className="mt-2 break-words text-sm font-black leading-6 text-white">{value}</div></div>; }
function Field({ label, value, onChange, type = "text", children }) { return <label className="grid gap-2"><span className="text-[10px] font-black uppercase tracking-[.16em] text-slate-500">{label}</span>{children || <input type={type} value={value ?? ""} onChange={(event) => onChange(event.target.value)} className="rounded-2xl border border-slate-200 bg-slate-50 px-4 py-3 text-sm font-black text-slate-950 outline-none focus:border-orange-400" />}</label>; }
function TextArea({ label, value, onChange }) { return <label className="grid gap-2 md:col-span-2"><span className="text-[10px] font-black uppercase tracking-[.16em] text-slate-500">{label}</span><textarea rows={3} value={value ?? ""} onChange={(event) => onChange(event.target.value)} className="rounded-2xl border border-slate-200 bg-slate-50 px-4 py-3 text-sm font-black text-slate-950 outline-none focus:border-orange-400" /></label>; }

function SettingsSlip({ section, reviewed, onClose, onReview }) {
  if (!section) return null;
  const isAnchor = String(section.href || "").startsWith("#");
  const openAnchor = () => {
    onClose();
    setTimeout(() => document.querySelector(section.href)?.scrollIntoView({ behavior: "smooth", block: "start" }), 50);
  };
  return <div className="fixed inset-0 z-[2147483600] overflow-y-auto bg-slate-950/92 p-3 text-white backdrop-blur-xl md:p-6" role="dialog" aria-modal="true"><div className="mx-auto flex min-h-[calc(100vh-24px)] max-w-6xl flex-col overflow-hidden rounded-[34px] border border-white/10 bg-gradient-to-br from-slate-950 via-slate-900 to-slate-950 shadow-2xl md:min-h-[calc(100vh-48px)]"><header className="flex items-start justify-between gap-4 border-b border-white/10 p-5 md:p-7"><div><div className="inline-flex rounded-full bg-amber-300/10 px-3 py-1 text-[10px] font-black uppercase tracking-[0.18em] text-amber-200">Settings slip</div><h2 className="mt-3 text-4xl font-black leading-[0.95] tracking-[-0.07em] text-white md:text-6xl">{section.title}</h2><p className="mt-4 max-w-3xl text-sm font-bold leading-6 text-slate-300 md:text-base">{section.copy}</p></div><button type="button" onClick={onClose} className="rounded-2xl bg-white px-4 py-3 text-sm font-black text-slate-950">Close</button></header><div className="grid flex-1 gap-5 p-5 md:grid-cols-[1.15fr_.85fr] md:p-7"><section className="rounded-[28px] border border-white/10 bg-white/[0.04] p-5"><div className="text-[10px] font-black uppercase tracking-[0.18em] text-amber-300">Review setting area</div><div className="mt-4 grid gap-3 md:grid-cols-2">{section.details.map(([label, value]) => <Detail key={label} label={label} value={value} />)}</div></section><aside className="rounded-[28px] border border-white/10 bg-white/[0.04] p-5"><div className="text-[10px] font-black uppercase tracking-[0.18em] text-cyan-200">Owner action</div><p className="mt-3 text-sm font-bold leading-6 text-slate-300">Open the real save area or related workspace. No dead settings buttons stay here.</p>{reviewed ? <div className="mt-4 rounded-3xl border border-emerald-300/25 bg-emerald-300/10 p-4 text-sm font-black text-emerald-100">Reviewed. This settings area has been checked.</div> : null}<div className="mt-5 grid gap-3"><button type="button" onClick={onReview} className="rounded-2xl bg-emerald-300 px-5 py-4 text-sm font-black text-slate-950">Mark setting reviewed</button>{isAnchor ? <button type="button" onClick={openAnchor} className="rounded-2xl bg-white px-5 py-4 text-sm font-black text-slate-950">Open save form</button> : <Link to={section.href} onClick={onClose} className="rounded-2xl bg-white px-5 py-4 text-center text-sm font-black text-slate-950 no-underline">Open related page</Link>}<button type="button" onClick={onClose} className="rounded-2xl bg-white/10 px-5 py-4 text-sm font-black text-white ring-1 ring-white/10">Back to settings</button></div></aside></div></div></div>;
}

export default function SettingsCommandPage() {
  const [selectedSection, setSelectedSection] = React.useState(null);
  const [settings, setSettings] = React.useState(() => loadBusinessSettings());
  const [reviewed, setReviewed] = React.useState(() => { try { return JSON.parse(localStorage.getItem("churvox_settings_reviewed") || "{}"); } catch { return {}; } });
  const completion = businessSettingsCompletion(settings);
  const selectedId = selectedSection?.id || "current";

  function markReviewed(section) { const next = { ...reviewed, [section.id]: true }; setReviewed(next); localStorage.setItem("churvox_settings_reviewed", JSON.stringify(next)); toast.success("Setting marked reviewed"); }
  function update(key, value) { setSettings((current) => ({ ...current, [key]: value })); }
  function save() { const saved = saveBusinessSettings(settings); setSettings(saved); toast.success("Business settings saved"); }
  function resetLocal() { if (!window.confirm("Reset local business settings? This only clears the local document/default settings used by forms.")) return; localStorage.removeItem("churvox_business_settings_v1"); const next = loadBusinessSettings(); setSettings(next); window.dispatchEvent(new CustomEvent("churvox-business-settings-updated", { detail: next })); toast.success("Local settings reset"); }
  function handleLogo(file) { if (!file) return; const reader = new FileReader(); reader.onload = () => update("logo_base64", String(reader.result || "")); reader.readAsDataURL(file); }

  const reviewedCount = sections.filter((section) => reviewed[section.id]).length;
  const missingLabels = completion.missing_fields.map((key) => completion.labels[key]).filter(Boolean);
  const legalLinks = [{ label: "Privacy Policy", to: "/privacy" }, { label: "Terms", to: "/terms" }, { label: "Account deletion", to: "/account-deletion" }, { label: "Support", to: "/support-board" }];

  return <main className={industrialPageShell} data-industrial-simple-page="settings" data-command-canvas><section className={`${industrialContentLane} space-y-5`}><DarkCard className="p-6 pl-9 md:p-8 md:pl-10"><span className={industrialChip}>Settings</span><h1 className="mt-5 max-w-4xl text-5xl font-black leading-[.9] tracking-[-.08em] text-white md:text-7xl">Set the business up once. Keep it clean.</h1><p className="mt-5 max-w-3xl text-sm font-bold leading-7 text-slate-300 md:text-base">Settings saves real business defaults used by quotes and invoices, plus links to plans, team, support and legal pages. No old integration clutter.</p><div className="mt-6 flex flex-wrap gap-3"><button type="button" onClick={() => document.querySelector("#business-settings")?.scrollIntoView({ behavior: "smooth", block: "start" })} className={`rounded-2xl px-5 py-3 text-sm font-black ${industrialAction}`}>Edit business settings</button><Link to="/plans" className={`rounded-2xl px-5 py-3 text-sm font-black ${industrialGhost}`}>View plans</Link><Link to="/support-board" className={`rounded-2xl px-5 py-3 text-sm font-black ${industrialGhost}`}>Get support</Link><Link to="/dashboard" className={`rounded-2xl px-5 py-3 text-sm font-black ${industrialGhost}`}>Command Board</Link></div></DarkCard>

    <section className="grid gap-5 md:grid-cols-3"><DarkCard color="#34d399"><div className="text-[10px] font-black uppercase tracking-[.2em] text-amber-300">Setup completion</div><div className="mt-3 text-5xl font-black tracking-[-.08em]">{completion.percent}%</div><p className="mt-2 text-sm font-bold text-slate-300">{completion.missing_count ? `${completion.missing_count} fields still missing.` : "Business settings complete."}</p></DarkCard><DarkCard color="#22d3ee"><div className="text-[10px] font-black uppercase tracking-[.2em] text-amber-300">Reviewed areas</div><div className="mt-3 text-5xl font-black tracking-[-.08em]">{reviewedCount}/{sections.length}</div><p className="mt-2 text-sm font-bold text-slate-300">Settings sections checked for launch.</p></DarkCard><DarkCard color="#facc15"><div className="text-[10px] font-black uppercase tracking-[.2em] text-amber-300">Documents</div><div className="mt-3 text-2xl font-black tracking-[-.05em]">{settings.invoice_prefix || "INV"} / {settings.quote_prefix || "QUO"}</div><p className="mt-2 text-sm font-bold text-slate-300">Invoice and quote defaults used by forms.</p></DarkCard></section>

    <section id="business-settings" className="rounded-[30px] border border-slate-200 bg-white p-5 shadow-[0_18px_50px_rgba(15,23,42,.07)] md:p-6"><div className="flex flex-wrap items-start justify-between gap-4"><div><div className="text-[10px] font-black uppercase tracking-[.2em] text-orange-600">Business settings form</div><h2 className="mt-2 text-3xl font-black tracking-[-.06em] text-slate-950">Real customer-facing defaults</h2><p className="mt-2 max-w-3xl text-sm font-bold leading-6 text-slate-600">These settings are saved locally and used by quote/invoice forms until a proven backend settings endpoint is live.</p></div>{settings.logo_base64 ? <img src={settings.logo_base64} alt="Business logo" className="h-16 w-16 rounded-2xl border border-slate-200 object-contain" /> : <div className="grid h-16 w-16 place-items-center rounded-2xl border border-slate-200 bg-slate-950 text-sm font-black text-white">CV</div>}</div><div className="mt-6 grid gap-4 md:grid-cols-2"><Field label="Business name" value={settings.business_name} onChange={(value) => update("business_name", value)} /><Field label="Trading name" value={settings.trading_name} onChange={(value) => update("trading_name", value)} /><Field label="Email" type="email" value={settings.email} onChange={(value) => update("email", value)} /><Field label="Phone" value={settings.phone} onChange={(value) => update("phone", value)} /><TextArea label="Business address" value={settings.business_address} onChange={(value) => update("business_address", value)} /><Field label="Website" value={settings.website} onChange={(value) => update("website", value)} /><Field label="GST number" value={settings.gst_number} onChange={(value) => update("gst_number", value)} /><Field label="NZBN" value={settings.nzbn} onChange={(value) => update("nzbn", value)} /><Field label="Bank account name" value={settings.bank_account_name} onChange={(value) => update("bank_account_name", value)} /><Field label="Bank account number" value={settings.bank_account_number} onChange={(value) => update("bank_account_number", value)} /><Field label="Invoice prefix" value={settings.invoice_prefix} onChange={(value) => update("invoice_prefix", value)} /><Field label="Quote prefix" value={settings.quote_prefix} onChange={(value) => update("quote_prefix", value)} /><Field label="GST rate %" type="number" value={settings.default_gst_rate} onChange={(value) => update("default_gst_rate", value)} /><Field label="Invoice due days" type="number" value={settings.default_invoice_due_days} onChange={(value) => update("default_invoice_due_days", value)} /><Field label="Quote expiry days" type="number" value={settings.default_quote_expiry_days} onChange={(value) => update("default_quote_expiry_days", value)} /><Field label="Trade / industry"><select value={settings.trade_industry_type || ""} onChange={(event) => update("trade_industry_type", event.target.value)} className="rounded-2xl border border-slate-200 bg-slate-50 px-4 py-3 text-sm font-black text-slate-950 outline-none focus:border-orange-400"><option value="">Choose industry</option>{industries.map((industry) => <option key={industry} value={industry}>{industry}</option>)}</select></Field><Field label="Service area / region" value={settings.service_area_region} onChange={(value) => update("service_area_region", value)} /><Field label="Working hours" value={settings.working_hours} onChange={(value) => update("working_hours", value)} /><TextArea label="Default customer message tone" value={settings.default_customer_message_tone} onChange={(value) => update("default_customer_message_tone", value)} /><Field label="Logo"><input type="file" accept="image/*" onChange={(event) => handleLogo(event.target.files?.[0])} className="rounded-2xl border border-slate-200 bg-slate-50 px-4 py-3 text-sm font-black text-slate-950" /></Field></div><div className="mt-5 flex flex-wrap gap-3"><button type="button" onClick={save} className="rounded-2xl bg-slate-950 px-5 py-4 text-sm font-black text-white">Save business settings</button><button type="button" onClick={resetLocal} className="rounded-2xl border border-slate-200 bg-white px-5 py-4 text-sm font-black text-slate-950">Reset local settings</button>{missingLabels.length ? <div className="rounded-2xl border border-amber-200 bg-amber-50 px-4 py-3 text-sm font-black text-amber-900">Missing: {missingLabels.slice(0, 4).join(", ")}{missingLabels.length > 4 ? "…" : ""}</div> : <div className="rounded-2xl border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm font-black text-emerald-900">Ready for customer-facing documents.</div>}</div></section>

    <section className="grid gap-5 xl:grid-cols-[minmax(0,1.1fr)_minmax(340px,.9fr)]"><section className="grid gap-4 md:grid-cols-2">{sections.map((section) => <DarkCard key={section.id} color={section.color} className="min-h-[180px]"><div className="flex items-start justify-between gap-3"><div><div className="text-[10px] font-black uppercase tracking-[.2em] text-amber-300">{section.badge}</div><h2 className="mt-2 text-2xl font-black tracking-[-.05em] text-white">{section.title}</h2></div>{reviewed[section.id] ? <span className="rounded-full bg-emerald-300 px-3 py-1 text-[10px] font-black uppercase tracking-[.14em] text-slate-950">Reviewed</span> : null}</div><p className="mt-3 text-sm font-bold leading-6 text-slate-300">{section.copy}</p><button type="button" onClick={() => setSelectedSection(section)} className="mt-4 inline-flex rounded-2xl bg-white px-4 py-3 text-sm font-black text-slate-950">Review slip</button></DarkCard>)}</section><aside className="rounded-[30px] border border-slate-200 bg-white p-5 shadow-[0_18px_50px_rgba(15,23,42,.07)] md:p-6"><div className="text-[10px] font-black uppercase tracking-[.2em] text-orange-600">Launch checklist</div><h2 className="mt-2 text-3xl font-black tracking-[-.06em]">Before going live</h2><p className="mt-2 text-sm font-bold leading-6 text-slate-600">{reviewedCount}/{sections.length} settings areas reviewed.</p><div className="mt-5 grid gap-3">{checklist.map((item) => <div key={item} className="flex items-start gap-3 rounded-2xl border border-slate-200 bg-slate-50 p-3"><span className="mt-0.5 grid h-6 w-6 shrink-0 place-items-center rounded-full bg-emerald-100 text-xs font-black text-emerald-700">✓</span><span className="text-sm font-black leading-6 text-slate-800">{item}</span></div>)}</div></aside></section>

    <section className="grid gap-5 lg:grid-cols-2"><section className="rounded-[30px] border border-slate-200 bg-white p-5 shadow-[0_18px_50px_rgba(15,23,42,.07)] md:p-6"><div className="text-[10px] font-black uppercase tracking-[.2em] text-cyan-700">Legal and support</div><h2 className="mt-2 text-3xl font-black tracking-[-.06em]">Live mobile-safe links</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{legalLinks.map((link) => <Link key={link.to} to={link.to} className="rounded-2xl border border-slate-200 bg-slate-50 px-5 py-4 text-center text-sm font-black text-slate-950 no-underline">{link.label}</Link>)}</div></section><section className="rounded-[30px] border border-slate-200 bg-white p-5 shadow-[0_18px_50px_rgba(15,23,42,.07)] md:p-6"><div className="text-[10px] font-black uppercase tracking-[.2em] text-cyan-700">Accounting direction</div><h2 className="mt-2 text-3xl font-black tracking-[-.06em]">Xero visible. Approval-first sync.</h2><p className="mt-3 max-w-3xl text-sm font-bold leading-7 text-slate-600">For launch polish, Churvox should talk about Xero where accounting is visible. Accounting sync should stay approval-first: Churvox prepares the admin, the owner approves it.</p><div className="mt-5 flex flex-wrap gap-3"><Link to="/support-board" className="rounded-2xl bg-slate-950 px-5 py-3 text-sm font-black text-white no-underline">Ask about Xero setup</Link><Link to="/invoices-board" className="rounded-2xl border border-slate-200 bg-white px-5 py-3 text-sm font-black text-slate-950 no-underline">Review invoices</Link></div></section></section>
  </section><SettingsSlip section={selectedSection} reviewed={Boolean(reviewed[selectedId])} onClose={() => setSelectedSection(null)} onReview={() => selectedSection && markReviewed(selectedSection)} /></main>;
}
