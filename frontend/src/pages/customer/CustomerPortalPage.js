import React, { useEffect, useState } from 'react';
import { useParams } from 'react-router-dom';
import { useApi } from '../../hooks/useApi';
export default function CustomerPortalPage(){const {token}=useParams(); const api=useApi(); const [data,setData]=useState(null); const [message,setMessage]=useState(''); useEffect(()=>{api.get(`/customer-portal/${token}`).then(r=>r.success&&setData(r.data?.data));},[token]); return <div className='min-h-screen bg-slate-50 p-6'><div className='mx-auto max-w-4xl rounded-3xl border bg-white p-6 shadow-sm'><h1 className='text-2xl font-black text-slate-950'>{data?.business_name || 'Customer Portal'}</h1><p className='text-slate-700'>{data?.customer_name}</p><pre className='mt-3 rounded-xl border p-3 text-xs'>{JSON.stringify(data,null,2)}</pre><textarea className='mt-3 w-full rounded-xl border p-2' placeholder='Message/request callback' value={message} onChange={(e)=>setMessage(e.target.value)}/><button className='mt-2 rounded-xl bg-blue-600 px-4 py-2 text-white' onClick={()=>api.post(`/customer-portal/${token}/message`,{message})}>Send message</button></div></div>;}
