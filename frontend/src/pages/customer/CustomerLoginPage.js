import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
export default function CustomerLoginPage(){const [email,setEmail]=useState(''); const [token,setToken]=useState(''); const [msg,setMsg]=useState(''); const nav=useNavigate(); return <div className='min-h-screen bg-slate-100 p-6'><div className='mx-auto max-w-xl rounded-3xl border bg-white p-6 shadow-sm'><h1 className='text-2xl font-black text-slate-950'>Customer login</h1><p className='mt-2 text-slate-700'>Customer account login is coming later. Use your secure portal link for now.</p><input className='mt-3 w-full rounded-xl border p-2' placeholder='Email' value={email} onChange={(e)=>setEmail(e.target.value)}/><input className='mt-3 w-full rounded-xl border p-2' placeholder='Portal token or code' value={token} onChange={(e)=>setToken(e.target.value)}/><button className='mt-3 rounded-xl bg-blue-600 px-4 py-2 font-bold text-white' onClick={()=>token.trim()?nav(`/customer/portal/${token.trim()}`):setMsg('Enter portal token/code to continue.')}>Open portal</button>{msg&&<p className='mt-2 text-sm text-slate-700'>{msg}</p>}</div></div>;}
