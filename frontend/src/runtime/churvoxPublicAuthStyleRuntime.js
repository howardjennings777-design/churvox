// CHURVOX_PUBLIC_AUTH_STYLE_RUNTIME_SHIM_20260705
// Safe paid-launch shim.
// The paid-launch index imports this runtime, but the file was missing from the restored tree.
// Keep this intentionally tiny so it cannot break public, auth, owner, worker, billing or accounting flows.

export {};
