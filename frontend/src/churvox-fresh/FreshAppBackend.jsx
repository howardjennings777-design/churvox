import React from "react";
import { useApi } from "../hooks/useApi";
import "../churvox-clean/ChurvoxCleanApp.css";
import "../churvox-clean/ChurvoxCleanDensity.css";
import "../churvox-clean/ChurvoxCleanPremium.css";

const nav = ["AI Guide", "Command", "Jobs", "Clients", "Quotes", "Invoices", "Team", "Payroll", "Workers", "Xero", "Settings", "Plans", "Support"];
const key = (value) => String(value || "").toLowerCase().replace(/\s+/g, "");
const aliases = { dashboard: "aiguide", smart: "aiguide", today: "aiguide", setup: "aiguide", guide: "aiguide", ai: "aiguide", automation: "command", approvals: "command", commanddesk: "command", recurring: "jobs", calendar: "jobs", schedule: "jobs", photos: "jobs", proof: "jobs", customers: "clients", documents: "clients", payments: "invoices", reports: "invoices", expenses: "invoices", profit: "invoices", staff: "team", people: "team", time: "payroll", timesheets: "payroll", dispatch: "workers", routes: "workers", worker: "workers", accounting: "xero", sync: "xero", integrations: "xero", security: "settings", launchcontrol: "settings", businessbranding: "settings", billing: "plans", help: "support" };
const titles = {
  aiguide: ["AI Guide", "This week, needs attention, quick actions, messages and quiet admin rules."],
  command: ["Command", "Owner approval desk. Approve, edit or park prepared admin."],
  jobs: ["Jobs", "Job cards, prices, dates, workers, recurring work, proof and job notes."],
  clients: ["Clients", "Client records, service memory, saved prices and job history."],
  quotes: ["Quotes", "Draft quotes, scopes, prices, terms, follow-ups and owner-approved sending."],
  invoices: ["Invoices", "Draft invoices, line items, evidence, paid status and guarded accounting handoff."],
  team: ["Team", "People, roles, access, invites and worker app status."],
  payroll: ["Payroll", "Timesheets, worker slips and pay-period review. No tax filing. No bank payout files."],
  workers: ["Workers", "Map, clock status, proof, messages, route view and daily slips."],
  xero: ["Xero", "Draft sync only. Owner-approved accounting handoff."],
  settings: ["Settings", "Business profile, GST, branding, imports, security and account controls."],
  plans: ["Plans", "Locked Churvox pricing and add-ons."],
  support: ["Support", "Setup help, short guides and contact options."],
};
const options = {
  repeat: ["One-off", "Weekly", "Fortnightly", "Monthly", "Custom"],
  service: ["Lawn mowing", "Hedge trimming", "Garden tidy", "Property reset", "Cleanup", "Quote visit", "Other"],
  billing: ["Fixed price", "Hourly", "Fixed + extras", "Hourly + extras", "Package price", "Quote required"],
  role: ["Owner", "Manager", "Worker", "Subcontractor", "Payroll only"],
  access: ["Full access", "Jobs only", "Worker app", "Payroll review", "No access"],
  invoiceStatus: ["Draft", "Due today", "Overdue", "Paid", "Ready draft"],
  quoteStatus: ["Draft", "Sent", "Viewed", "Accepted", "Follow-up due"],
};
const actions = { command: ["Approve selected", "Edit selected", "Park selected", "Limit waiting list to 5"], jobs: ["Add job", "Recurring setup", "Assign worker", "Open editable form"], clients: ["Add client", "CSV import", "Open client form", "View job history"], quotes: ["Create quote", "Follow up", "Convert to job", "Send after approval"], invoices: ["Create invoice", "Review draft", "Sync draft", "Refresh paid status"], team: ["Add person", "Invite worker", "Set access", "Worker app status"], payroll: ["Weekly", "Fortnightly", "Monthly", "CSV export"], workers: ["Open map", "Send message", "Review proof", "Daily slips"], xero: ["Check status", "Draft sync", "Refresh payments", "Disconnect"], settings: ["Save profile", "Upload logo", "Import clients", "Security"], support: ["Setup guide", "Contact help", "Report issue"] };
const planRows = [["Start", "$39", "For a solo owner starting clean.", ["Clients", "Jobs", "Quotes", "Invoices"]], ["Crew", "$89", "For a small crew that needs jobs, clients and workers.", ["Team access", "Worker slips", "Recurring jobs", "Command basics"]], ["Operator", "$149", "Most Popular. Churvox prepares more admin for approval.", ["AI admin preparation", "Approval desk", "Follow-ups", "Better daily control"]], ["Command", "$299", "Full approval desk, scale and accounting handoff.", ["Xero/MYOB draft sync", "Advanced controls", "Scale features", "Owner guardrails"]], ["Growth Pack", "$99", "Adds 50 active team members and extra capacity.", ["50 active team members", "Extra job capacity", "More AI actions", "More automation capacity"]], ["Accounting Add-on", "$39", "For non-Command tiers that need accounting handoff.", ["Draft sync only", "No auto-send", "No tax filing", "No payout files"]]];

const fallback = {
  jobs: [{ type: "job", title: "Lawn care - Birchville", client: "Lisa M", price: "$85", amount: "$85", status: "Ready", date: "Today", time: "10:30", duration: "1.5h", worker: "Worker account", repeat: "Fortnightly", address: "Birchville, Lower Hutt, New Zealand", service: "Lawn mowing", billing: "Fixed price", proof: "Before/after photos required before complete", issue: "No issue", note: "Mow, edges, blower tidy. Worker proof required before invoice draft." }],
  clients: [{ type: "client", title: "Lisa M", name: "Lisa M", client: "Birchville", phone: "", email: "", price: "$85 saved", status: "Active", date: "Fortnightly", repeat: "Fortnightly", address: "Birchville, Lower Hutt, New Zealand", service: "Lawn mowing", note: "Saved price, service memory and invoice history.", history: ["Last job completed", "Invoice draft ready", "Next job booked"] }],
  quotes: [{ type: "quote", title: "Garden reset quote", client: "Robert K", price: "$190", amount: "$190", status: "Draft", date: "Prepared today", scope: "Overgrown reset, green waste, tidy and photo proof.", terms: "Owner approval before sending.", followUp: "Follow up tomorrow.", next: "Send to Command for approval", note: "Scope, price and follow-up are ready for Command approval." }],
  invoices: [{ type: "invoice", title: "INV-1042", client: "Lisa M", job: "Lawn care - Birchville", price: "$85", amount: "$85", status: "Draft", date: "Due today", due: "7 days", line: "Lawn mowing, edging and blower tidy", evidence: "Worker proof ready", sync: "Waiting for owner approval before draft sync", note: "Review before sending. No automatic invoice sending." }],
  team: [{ type: "worker", title: "Worker account", name: "Worker account", client: "Field worker", price: "Worker app", status: "Invited", date: "Mobile access", role: "Worker", access: "Worker app", app: "Invite sent", payroll: "Clock proof required", note: "Jobs, proof, messages, clock status and daily slips only." }],
  messages: [{ type: "message", title: "Side hedge question", from: "Worker account", client: "Mere H", job: "Hedge trim", channel: "Worker app", priority: "Owner decision", status: "Waiting", date: "Today", detail: "Should I trim the side hedge as well or just the front?", draft: "Please do the front hedge only for now.", history: "Worker asked from site before starting.", note: "Message is in Command so the job page stays clean." }],
  command: [{ type: "command", title: "Approve garden reset quote", client: "Robert K", price: "$190", amount: "$190", status: "Approval", date: "Today", owner: "Approve", evidence: "Scope prepared from live record.", note: "Owner can approve, edit the slip, or park it.", slip: ["Client confirmed reset", "Green waste included", "Photos required before invoice"] }],
};

function startPage() { if (typeof window === "undefined") return "aiguide"; const raw = (window.location.hash || window.location.pathname || "").replace("#", "").replace(/^\/+/, "").split("/")[0].toLowerCase(); return aliases[raw] || (nav.map(key).includes(raw) ? raw : "aiguide"); }
function text(...values) { for (const value of values) if (value !== undefined && value !== null && String(value).trim()) return String(value).trim(); return ""; }
function rawId(record) { const value = record?.id || record?._id || record?.job_id || record?.client_id || record?.invoice_id || record?.quote_id || record?.user_id || record?.action_id || ""; if (typeof value === "object") return String(value.$oid || value.oid || value.id || value._id || ""); return String(value || ""); }
function realId(record) { const id = rawId(record); return /^(job|client|quote|invoice|worker|message|command|slip)-/i.test(id) ? "" : id; }
function money(value) { if (typeof value === "string" && value.trim().startsWith("$")) return value; const number = Number(value || 0); return number ? number.toLocaleString("en-NZ", { style: "currency", currency: "NZD", maximumFractionDigits: 0 }) : ""; }
function listFrom(result, keyName) { const body = result?.value?.data || result?.data || result?.value || result; const data = body?.data ?? body; if (Array.isArray(data)) return data; if (keyName && Array.isArray(data?.[keyName])) return data[keyName]; if (keyName && Array.isArray(data?.data?.[keyName])) return data.data[keyName]; for (const name of [keyName, "items", "records", "results", "jobs", "clients", "quotes", "invoices", "workers", "team", "messages", "actions", "notifications", "data"]) if (name && Array.isArray(data?.[name])) return data[name]; return []; }
function keep(mapped, fallbackRows) { return mapped.length ? mapped : fallbackRows; }
function mapJob(row, index) { const amount = row.price ?? row.amount ?? row.total; return { ...row, type: "job", id: realId(row) || `job-${index}`, title: text(row.title, row.job_title, row.job_name, row.description, `Job ${index + 1}`), client: text(row.client_name, row.customer_name, row.client?.name, row.client, "No client"), price: money(amount), amount: money(amount), status: text(row.status, row.job_status, row.stage, "Assigned"), date: text(row.scheduled_date, row.date, "Not scheduled"), time: text(row.scheduled_time, row.start_time, row.time, ""), duration: text(row.duration, row.estimated_duration, ""), worker: text(row.assigned_worker_name, row.worker_name, row.worker?.name, "Unassigned"), repeat: text(row.recurring, row.repeat, row.frequency, "One-off"), address: text(row.address, row.site_address, row.location, ""), service: text(row.service, row.service_type, "Other"), billing: text(row.billing, row.billing_type, "Fixed price"), proof: text(row.proof, row.photo_status, row.evidence, "Proof not attached"), issue: text(row.issue, row.problem, row.needs_attention, "No issue"), note: text(row.notes, row.note, row.description, "Open the form to review this job.") }; }
function mapClient(row, index) { return { ...row, type: "client", id: realId(row) || `client-${index}`, title: text(row.name, row.client_name, row.customer_name, `Client ${index + 1}`), name: text(row.name, row.client_name, row.customer_name, `Client ${index + 1}`), client: text(row.area, row.suburb, row.address, "Client"), phone: text(row.phone, row.mobile, row.phone_number, ""), email: text(row.email, ""), price: text(row.default_price, row.saved_price, row.price, ""), status: text(row.status, "Active"), date: text(row.schedule, row.frequency, row.repeat, ""), repeat: text(row.schedule, row.frequency, row.repeat, "One-off"), address: text(row.address, row.site_address, ""), service: text(row.service, row.preferred_service, "Other"), note: text(row.notes, row.note, "Saved client record."), history: ["Job history", "Quotes", "Invoices"] }; }
function mapQuote(row, index) { const amount = row.amount ?? row.total ?? row.price; return { ...row, type: "quote", id: realId(row) || `quote-${index}`, title: text(row.title, row.quote_number, row.number, `Quote ${index + 1}`), client: text(row.client_name, row.customer_name, row.client?.name, row.client, "No client"), price: money(amount), amount: money(amount), status: text(row.status, "Draft"), date: text(row.date, row.created_at, row.updated_at, "Prepared"), scope: text(row.scope, row.description, row.notes, "Scope ready for review."), terms: text(row.terms, "Owner approval before sending."), followUp: text(row.follow_up, row.followUp, ""), next: text(row.next_step, row.next, "Waiting in Command"), note: text(row.notes, row.note, row.description, "Quote ready for owner review.") }; }
function mapInvoice(row, index) { const amount = row.amount ?? row.total ?? row.price; return { ...row, type: "invoice", id: realId(row) || `invoice-${index}`, title: text(row.invoice_number, row.number, row.title, `Invoice ${index + 1}`), client: text(row.client_name, row.customer_name, row.client?.name, row.client, "No client"), job: text(row.job_title, row.job, row.description, ""), price: money(amount), amount: money(amount), status: text(row.status, "Draft"), date: text(row.due_date, row.date, row.created_at, ""), due: text(row.due_date, row.due, ""), line: text(row.line_item, row.description, row.notes, "Invoice line item"), evidence: text(row.evidence, row.proof, "Owner review required"), sync: text(row.accounting_status, row.sync, row.xero_status, "Draft sync only"), note: text(row.notes, row.note, row.description, "Invoice draft ready for review.") }; }
function mapWorker(row, index) { return { ...row, type: row.type || "worker", id: realId(row) || `worker-${index}`, title: text(row.name, row.full_name, row.email, row.title, `Worker ${index + 1}`), name: text(row.name, row.full_name, row.email, `Worker ${index + 1}`), client: text(row.role, row.access, "Worker"), price: text(row.payroll_status, row.payroll, ""), status: text(row.status, row.clock_status, row.invite_status, "Active"), date: text(row.current_job, row.job_title, row.app_status, "Worker app"), worker: text(row.name, row.full_name, row.email, "Worker"), role: text(row.role, "Worker"), access: text(row.access, row.role, "Worker app"), app: text(row.app_status, row.invite_status, "Worker app"), payroll: text(row.payroll_status, row.payroll, "Review"), gps: text(row.gps, row.location, row.address, ""), start: text(row.clock_in, row.start, ""), end: text(row.clock_out, row.end, ""), timesheet: text(row.timesheet, row.hours_today, row.duration, "No time recorded"), proof: text(row.proof, row.photo_status, "No proof yet"), messages: text(row.messages, row.message_status, row.issue, "No messages"), slip: text(row.slip, row.pay_slip_status, "Payroll review"), note: text(row.notes, row.note, "Worker record.") }; }
function mapMessage(row, index) { return { ...row, type: "message", id: realId(row) || `message-${index}`, title: text(row.subject, row.title, row.event_type, `Message ${index + 1}`), from: text(row.from, row.type, "Churvox"), client: text(row.client, row.client_name, row.customer_name, "Business"), job: text(row.job, row.job_title, row.route, "Churvox"), channel: text(row.channel, "Owner notification"), priority: text(row.priority, row.read ? "Read" : "Unread"), status: text(row.status, row.read ? "Read" : "Waiting"), date: text(row.created_at, row.updated_at, "Recent"), detail: text(row.detail, row.message, row.body, "Owner update ready."), draft: text(row.draft, row.reply, "Review this update in Command if needed."), history: text(row.history, row.created_at, "Recent"), note: text(row.note, row.message, "Message ready for review.") }; }
function mapCommand(row, index) { const amount = row.amount ?? row.total ?? row.payload?.amount ?? row.payload?.price; return { ...row, type: row.type === "message" ? "message" : "command", id: realId(row) || `command-${index}`, title: text(row.title, row.summary, row.label, "Prepared admin item"), client: text(row.client, row.client_name, row.customer_name, row.payload?.client_name, "Business"), price: money(amount), amount: money(amount), status: text(row.status, "Review"), date: text(row.created_at, row.updated_at, "Today"), owner: text(row.owner, row.recommended_action, "Approve"), evidence: text(row.evidence, row.reason, "Prepared from live business records."), note: text(row.filled, row.description, row.summary, "Prepared for owner review."), slip: [text(row.check, row.owner_check, "Approve, edit or park in Command.")] }; }
function makeWorkerSlips(workers, jobs) { const base = workers.length ? workers : jobs.map((job, index) => ({ ...job, id: `slip-${index}`, title: `${job.client} daily slip`, name: job.worker, gps: job.address, timesheet: job.duration, messages: job.issue, slip: "Feeds payroll review" })); return base.map(mapWorker); }
function metrics(data) { const drafts = data.invoices.filter((i) => /draft|ready/i.test(i.status || "")).length; return [["Today", `${data.jobs.length} jobs`, "Live jobs from backend or fallback"], ["This week", `${data.quotes.length + data.invoices.length} records`, "Quotes and invoices Churvox can prepare"], ["Needs attention", `${data.command.length}`, "Owner decisions only"], ["Invoices", `${drafts} drafts`, "No automatic sending"]]; }
function useBackendData(api) { const [data, setData] = React.useState({ ...fallback, workerSlips: fallback.team.map(mapWorker), command: fallback.command, metrics: metrics({ ...fallback, command: fallback.command }), xero: { connected: false, tenant_name: "" }, backend: "loading" }); const reload = React.useCallback(async () => { const res = await Promise.allSettled([api.get("/jobs"), api.get("/clients"), api.get("/team"), api.get("/quotes"), api.get("/invoices"), api.get("/messages"), api.get("/ai/actions"), api.get("/xero/status")]); const jobs = keep(listFrom(res[0], "jobs").map(mapJob), fallback.jobs); const clients = keep(listFrom(res[1], "clients").map(mapClient), fallback.clients); const team = keep(listFrom(res[2], "team").map(mapWorker), fallback.team); const quotes = keep(listFrom(res[3], "quotes").map(mapQuote), fallback.quotes); const invoices = keep(listFrom(res[4], "invoices").map(mapInvoice), fallback.invoices); const messages = keep(listFrom(res[5], "messages").map(mapMessage), fallback.messages); const command = keep([...listFrom(res[6], "actions").map(mapCommand), ...messages.filter((m) => /waiting|unread|draft/i.test(m.status || ""))], fallback.command).slice(0, 12); const xeroRaw = res[7]?.value?.data?.data || res[7]?.value?.data || res[7]?.value || {}; const next = { jobs, clients, team, quotes, invoices, messages, command, workerSlips: makeWorkerSlips(team, jobs), xero: { connected: Boolean(xeroRaw.connected || xeroRaw.xero_connected), tenant_name: text(xeroRaw.tenant_name, xeroRaw.tenantName, "") }, backend: res.some((r) => r.status === "fulfilled" && r.value?.success !== false) ? "live" : "fallback" }; next.metrics = metrics(next); setData(next); }, [api]); React.useEffect(() => { reload(); window.addEventListener("churvox:fresh-data-updated", reload); return () => window.removeEventListener("churvox:fresh-data-updated", reload); }, [reload]); return { data, reload }; }

function ActionStrip({ page, items }) { const rows = items || actions[page] || []; return <div className="cvxActions">{rows.map((item, index) => <button key={item} type="button"><b>{index === 0 ? "+" : index === 1 ? "✓" : "•"}</b>{item}</button>)}</div>; }
function Panel({ eyebrow, title, children, wide }) { return <section className={`cvxPanel ${wide ? "cvxWide" : ""}`}><p className="cvxEyebrow">{eyebrow}</p><h2>{title}</h2>{children}</section>; }
function Metrics({ items }) { return <div className="cvxMetrics">{items.map(([label, value, note]) => <article key={label}><span>{label}</span><b>{value}</b><small>{note}</small></article>)}</div>; }
function RecordCard({ item, onOpen }) { return <button className="cvxRecordCard" type="button" onClick={() => onOpen(item)}><span className="cvxPill">{item.status}</span><h3>{item.title}</h3><p>{item.note}</p><div className="cvxRecordMeta"><b>{item.client || item.from}</b><strong>{item.price || item.amount || ""}</strong><small>{item.date}</small><small>{item.repeat || item.channel || item.access}</small></div></button>; }
function RecordGrid({ items, onOpen, limited }) { return <div className={`cvxRecordGrid ${limited ? "cvxLimited" : ""}`}>{items.map((item) => <RecordCard key={`${item.id || item.title}-${item.client || item.from}`} item={item} onOpen={onOpen} />)}</div>; }
function MiniList({ items }) { return <div className="cvxMiniList">{items.map((item) => <div key={Array.isArray(item) ? item.join("-") : item}><b>{Array.isArray(item) ? item[0] : item}</b>{Array.isArray(item) && <span>{item.slice(1).join(" · ")}</span>}</div>)}</div>; }
function FieldForm({ fields, button }) { return <div className="cvxFormGrid">{fields.map(([label, value, type, choices]) => <label key={label}>{label}{type === "textarea" ? <textarea defaultValue={value} /> : type === "select" ? <select defaultValue={value}>{(choices || options.repeat).map((choice) => <option key={choice}>{choice}</option>)}</select> : <input defaultValue={value} />}</label>)}<button type="button">{button}</button></div>; }
function GoogleMap({ query }) { const q = query || "Lower Hutt New Zealand"; return <div className="cvxGoogleMap"><iframe title="Churvox worker map" src={`https://www.google.com/maps?q=${encodeURIComponent(q)}&output=embed`} loading="lazy" referrerPolicy="no-referrer-when-downgrade" /><a href={`https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(q)}`} target="_blank" rel="noreferrer">Open in Google Maps</a></div>; }
function ProofGrid() { return <div className="cvxProofGrid"><article><b>Before photo</b><span>Required before completing selected jobs.</span></article><article><b>After photo</b><span>Attached to job, invoice and client history.</span></article><article><b>Issue photo</b><span>Anything unclear goes to Command for owner decision.</span></article></div>; }
function detailFields(item) { if (item.type === "client") return [["title", "Name"], ["phone", "Phone"], ["email", "Email"], ["address", "Address"], ["service", "Preferred service", "select", options.service], ["price", "Saved price"], ["repeat", "Preferred schedule", "select", options.repeat], ["note", "Access notes", "textarea"]]; if (item.type === "worker" || item.type === "person") return [["name", "Worker / person"], ["role", "Role", "select", options.role], ["access", "Access", "select", options.access], ["email", "Phone / email"], ["status", "Clock status"], ["title", "Current job"], ["gps", "GPS / location"], ["start", "Clock in"], ["end", "Clock out"], ["proof", "Proof / photos"], ["messages", "Worker messages"], ["timesheet", "Timesheet"], ["slip", "Slip / payroll status"], ["note", "Day notes", "textarea"]]; if (item.type === "quote") return [["title", "Quote"], ["client", "Client"], ["amount", "Amount"], ["status", "Status", "select", options.quoteStatus], ["scope", "Scope", "textarea"], ["terms", "Terms", "textarea"], ["followUp", "Follow-up"], ["next", "Next step"], ["note", "Notes", "textarea"]]; if (item.type === "invoice") return [["title", "Invoice"], ["client", "Client"], ["job", "Job"], ["amount", "Amount"], ["due", "Due date"], ["status", "Status", "select", options.invoiceStatus], ["sync", "Xero/MYOB status"], ["line", "Line item", "textarea"], ["evidence", "Evidence", "textarea"], ["note", "Notes", "textarea"]]; if (item.type === "message") return [["from", "From"], ["channel", "Channel"], ["client", "Client"], ["job", "Job"], ["title", "Subject"], ["priority", "Priority"], ["history", "History", "textarea"], ["detail", "Message", "textarea"], ["draft", "Drafted reply", "textarea"], ["note", "Owner note", "textarea"]]; if (item.type === "command") return [["title", "Record"], ["client", "Client"], ["amount", "Amount"], ["status", "Prepared status"], ["owner", "Recommended action", "select", ["Approve", "Edit", "Park"]], ["evidence", "Evidence checked", "textarea"], ["note", "What Churvox filled", "textarea"]]; return [["title", "Job name"], ["client", "Client"], ["address", "Site address"], ["service", "Service", "select", options.service], ["worker", "Assigned worker"], ["date", "Scheduled date"], ["time", "Start time"], ["duration", "Estimated duration"], ["amount", "Price NZD"], ["billing", "Billing type", "select", options.billing], ["repeat", "Frequency", "select", options.repeat], ["status", "Status"], ["proof", "Proof/photos"], ["issue", "Issue status"], ["note", "Job notes", "textarea"]]; }
function payloadFor(item, form, action) { const common = { ...form, owner_action: action }; if (item.type === "job") return { title: form.title, client_name: form.client, address: form.address, service: form.service, assigned_worker_name: form.worker, scheduled_date: form.date, scheduled_time: form.time, duration: form.duration, price: form.amount || form.price, billing: form.billing, recurring: form.repeat, status: form.status, proof: form.proof, notes: form.note }; if (item.type === "client") return { name: form.title || form.name, phone: form.phone, email: form.email, address: form.address, service: form.service, price: form.price, schedule: form.repeat, notes: form.note }; if (item.type === "invoice") return { client_name: form.client, job_title: form.job, amount: form.amount, due_date: form.due, status: form.status, accounting_status: form.sync, line_item: form.line, evidence: form.evidence, notes: form.note }; if (item.type === "quote") return { title: form.title, client_name: form.client, amount: form.amount, status: form.status, scope: form.scope, terms: form.terms, follow_up: form.followUp, next_step: form.next, notes: form.note }; if (item.type === "worker" || item.type === "person") return { name: form.name || form.title, role: form.role, access: form.access, email: form.email, status: form.status, current_job: form.title, gps: form.gps, clock_in: form.start, clock_out: form.end, proof: form.proof, messages: form.messages, timesheet: form.timesheet, notes: form.note }; return common; }
async function firstGood(calls) { let last = "Could not save yet."; for (const call of calls) { const res = await call(); if (res?.success !== false) return res; last = res?.error || last; } throw new Error(last); }
function Drawer({ item, page, onClose, api, reload }) { const [form, setForm] = React.useState(item || {}); const [notice, setNotice] = React.useState(""); const [busy, setBusy] = React.useState(false); React.useEffect(() => { setForm(item || {}); setNotice(""); }, [item]); if (!item) return null; const commandMode = page === "command" || item.type === "command" || item.type === "message"; const set = (field, value) => setForm((current) => ({ ...current, [field]: value })); async function save(action = "save") { setBusy(true); setNotice(""); const id = realId(item); const payload = payloadFor(item, form, action); try { if (item.type === "job" && id) await firstGood([() => api.patch(`/jobs/${id}`, payload), () => api.patch(`/jobs/${id}/field-update`, payload)]); else if (item.type === "client" && id) await firstGood([() => api.patch(`/clients/${id}`, payload), () => api.put(`/clients/${id}`, payload)]); else if (item.type === "invoice" && id) await firstGood([() => api.patch(`/invoices/${id}`, payload), () => api.put(`/invoices/${id}`, payload)]); else if (item.type === "quote" && id) await firstGood([() => api.patch(`/quotes/${id}`, payload), () => api.put(`/quotes/${id}`, payload)]); else if ((item.type === "worker" || item.type === "person") && id) await firstGood([() => api.patch(`/team/workers/${id}`, payload), () => api.patch(`/team/${id}`, payload)]); else await api.post("/command/execute-approved", { kind: item.type || "record", action, item: { ...item, fields: form, payload } }); if (commandMode) await api.post("/command/execute-approved", { kind: item.type || "command_record", action, item: { ...item, fields: form, payload } }); setNotice(action === "park" ? "Parked in Command." : action === "approve" ? "Approved/saved." : "Saved."); await reload(); } catch (error) { setNotice(error?.message || "Could not save yet."); } finally { setBusy(false); } } return <aside className="cvxDrawer"><button className="cvxClose" type="button" onClick={onClose}>Close</button><p className="cvxEyebrow">{commandMode ? "Owner decision slip" : realId(item) ? "Editable live record" : "Example record"}</p><h2>{form.title || form.name}</h2><div className="cvxDrawerFields">{detailFields(item).map(([field, label, type, choices]) => <label key={field} className={type === "textarea" ? "cvxFullField" : ""}>{label}{type === "textarea" ? <textarea disabled={busy} value={form[field] || ""} onChange={(e) => set(field, e.target.value)} /> : type === "select" ? <select disabled={busy} value={form[field] || ""} onChange={(e) => set(field, e.target.value)}><option value="">Select</option>{(choices || []).map((choice) => <option key={choice}>{choice}</option>)}</select> : <input disabled={busy} value={form[field] || ""} onChange={(e) => set(field, e.target.value)} />}</label>)}</div>{(form.slip || form.history) && <div className="cvxSlipBox"><b>{form.slip ? "Prepared slip" : "History"}</b><ul>{(form.slip || form.history || []).map((line) => <li key={line}>{line}</li>)}</ul></div>}{notice ? <p className="cvxNotice">{notice}</p> : null}<div className="cvxDrawerActions">{commandMode ? <><button disabled={busy} type="button" onClick={() => save("approve")}>Approve</button><button disabled={busy} type="button" onClick={() => save("edit")}>Edit</button><button disabled={busy} type="button" onClick={() => save("park")}>Park</button></> : <><button disabled={busy} type="button" onClick={() => save("save")}>Save form</button><button disabled={busy} type="button" onClick={() => save("park")}>Send issue to Command</button></>}</div><small>{realId(item) ? "Loaded from backend. Changes save back to the backend where the endpoint supports it." : "Example record. Live records replace this after backend data loads."}</small></aside>; }

function AIGuide({ data }) { return <div className="cvxPageGrid"><Panel eyebrow="This week" title="Business pulse" wide><Metrics items={data.metrics} /></Panel><Panel eyebrow="Needs attention" title="Owner decisions"><MiniList items={data.command.slice(0, 4).map((i) => [i.title, i.client, i.status])} /></Panel><Panel eyebrow="Quick actions" title="Start the admin"><ActionStrip items={["Add job", "Add client", "Create quote", "Create invoice", "Open Command", "Import clients"]} /></Panel><Panel eyebrow="Messages" title="Worker and customer threads"><MiniList items={data.messages.slice(0, 4).map((m) => [m.title, m.client, m.status])} /></Panel><Panel eyebrow="Proof" title="Photos and evidence"><ProofGrid /></Panel><Panel eyebrow="Quiet admin rules" title="Churvox does the admin. You approve."><ul className="cvxRules"><li>Issues go to Command, not hidden inside Jobs.</li><li>Draft quotes and invoices wait for owner approval.</li><li>Recurring work is saved on the job form.</li><li>Paid is only updated after accounting refresh confirms paid.</li></ul></Panel></div>; }
function CommandPage({ data, open }) { return <div className="cvxPageGrid"><Panel eyebrow="Decision desk" title="Waiting for owner" wide><RecordGrid items={data.command} onOpen={open} limited /></Panel><Panel eyebrow="Command controls" title="Approve, edit or park"><ActionStrip page="command" /><p className="cvxNotice">Only Command shows approval actions. Other pages send issues here.</p></Panel><Panel eyebrow="Messages" title="Replies waiting for approval" wide><RecordGrid items={data.messages} onOpen={open} /></Panel></div>; }
function JobsPage({ data, open }) { return <div className="cvxPageGrid"><Panel eyebrow="Job board" title="Saved jobs" wide><RecordGrid items={data.jobs} onOpen={open} /></Panel><Panel eyebrow="Add / edit job" title="Full job form"><FieldForm button="Create job later" fields={[["Client", ""], ["Job title", ""], ["Service", "Lawn mowing", "select", options.service], ["Billing", "Fixed price", "select", options.billing], ["Price", ""], ["Date", ""], ["Time", ""], ["Duration", ""], ["Worker", ""], ["Repeat", "Fortnightly", "select", options.repeat], ["Proof/photos", ""], ["Notes", "", "textarea"]]} /></Panel><Panel eyebrow="Proof" title="Photos and issue flow"><ProofGrid /></Panel><Panel eyebrow="Rules" title="Keep Jobs clean"><ul className="cvxRules"><li>Jobs is for the work, not owner problems.</li><li>Any price, proof, message or schedule issue goes to Command.</li><li>Recurring stays on the job form.</li></ul></Panel></div>; }
function ClientsPage({ data, open }) { return <div className="cvxPageGrid"><Panel eyebrow="Client records" title="Saved clients" wide><RecordGrid items={data.clients} onOpen={open} /></Panel><Panel eyebrow="Add client" title="Client form"><FieldForm button="Create client later" fields={[["Name", ""], ["Phone", ""], ["Email", ""], ["Address", ""], ["Preferred service", "Lawn mowing", "select", options.service], ["Default price", ""], ["Default repeat", "Fortnightly", "select", options.repeat], ["Access notes", "", "textarea"]]} /></Panel><Panel eyebrow="CSV import" title="Bring clients in"><p className="cvxNotice">Import area stays visible here so it does not get lost.</p><ActionStrip items={["Choose CSV", "Check headers", "Import clients"]} /></Panel><Panel eyebrow="Job history" title="Client memory" wide><MiniList items={data.clients.map((client) => [client.title, ...(client.history || [client.service, client.price])])} /></Panel></div>; }
function QuotesPage({ data, open }) { return <div className="cvxPageGrid"><Panel eyebrow="Quote drafts" title="Ready for owner" wide><RecordGrid items={data.quotes} onOpen={open} /></Panel><Panel eyebrow="Create quote" title="Scope, terms and price"><FieldForm button="Prepare quote later" fields={[["Client", ""], ["Scope", "", "textarea"], ["Price", ""], ["Terms", "Valid for 14 days", "textarea"], ["Follow-up date", ""], ["Convert repeat", "One-off", "select", options.repeat]]} /></Panel><Panel eyebrow="Follow-up" title="Churvox prepares it"><MiniList items={[["Draft reply", "Polite follow-up waits for owner approval"], ["Convert to job", "Only after quote is accepted"], ["Send rule", "No automatic sending"]]} /></Panel></div>; }
function InvoicesPage({ data, open }) { return <div className="cvxPageGrid"><Panel eyebrow="Invoice drafts" title="Review before sending" wide><RecordGrid items={data.invoices} onOpen={open} /></Panel><Panel eyebrow="Create invoice" title="From job slip"><FieldForm button="Prepare invoice later" fields={[["Client", ""], ["Job", ""], ["Amount", ""], ["Line item", "", "textarea"], ["Evidence", "", "textarea"], ["Due date", "7 days"], ["Status", "Draft", "select", options.invoiceStatus]]} /></Panel><Panel eyebrow="Paid guardrail" title="No false paid status"><ul className="cvxRules"><li>No automatic invoice sending.</li><li>No paid marking from a button alone.</li><li>Paid only after Xero/MYOB refresh confirms paid.</li><li>Draft sync only after owner approval.</li></ul></Panel></div>; }
function TeamPage({ data, open }) { return <div className="cvxPageGrid"><Panel eyebrow="People" title="Team and access" wide><RecordGrid items={data.team} onOpen={open} /></Panel><Panel eyebrow="Invite" title="Add worker"><FieldForm button="Invite worker later" fields={[["Name", ""], ["Email", ""], ["Role", "Worker", "select", options.role], ["Access", "Worker app", "select", options.access], ["Phone", ""], ["Notes", "", "textarea"]]} /></Panel><Panel eyebrow="Access rules" title="Keep roles simple"><MiniList items={[["Owner", "Command, settings, billing, approvals"], ["Worker", "Assigned jobs, photos, messages, clock"], ["Payroll", "Pay review only, no reports unless allowed"]]} /></Panel></div>; }
function PayrollPage({ data, open }) { return <div className="cvxPageGrid"><Panel eyebrow="Pay period" title="Review only"><ActionStrip page="payroll" /><p className="cvxNotice">Payroll is review/export only. No tax filing and no bank payout files.</p></Panel><Panel eyebrow="This period" title="Worker hours" wide><div className="cvxTable">{data.workerSlips.map((row) => <div key={row.id}><b>{row.name || row.worker}</b><span>{row.timesheet || "0h"}</span><span>{row.price || "Review"}</span><small>{row.status}</small><small>{row.proof || row.slip}</small></div>)}</div></Panel><Panel eyebrow="Worker slips" title="Clock proof feeds payroll" wide><RecordGrid items={data.workerSlips} onOpen={open} /></Panel></div>; }
function WorkersPage({ data, open }) { const mapQuery = data.jobs.map((job) => job.address).filter(Boolean).slice(0, 5).join(" ") || "Lower Hutt New Zealand"; return <div className="cvxPageGrid"><Panel eyebrow="GPS map" title="Worker route view" wide><GoogleMap query={mapQuery} /></Panel><Panel eyebrow="Daily slips" title="Clock, proof and messages" wide><RecordGrid items={data.workerSlips} onOpen={open} /></Panel><Panel eyebrow="Proof" title="Photos before complete"><ProofGrid /></Panel><Panel eyebrow="Messages" title="Sent to Command"><MiniList items={[["Issue", "Worker questions become owner decisions"], ["Photos", "Before and after proof stays with the job"], ["Clock", "Start, pause, resume and complete feeds payroll review"]]} /></Panel></div>; }
function XeroPage({ data }) { return <div className="cvxPageGrid"><Panel eyebrow="Guarded sync" title="Xero / MYOB handoff"><ActionStrip page="xero" /><ul className="cvxRules"><li>Draft invoice sync only.</li><li>Owner approval required.</li><li>No automatic invoice sending.</li><li>No tax filing.</li><li>No bank payout files.</li></ul></Panel><Panel eyebrow="Payment refresh" title="Paid only when confirmed"><p className="cvxNotice">{data.xero.connected ? `Connected to ${data.xero.tenant_name || "accounting"}. Paid status still only changes after refresh confirms paid.` : "Not connected here yet. Guardrails stay visible."}</p><Metrics items={[["Connection", data.xero.connected ? "Live" : "Later", data.xero.tenant_name || "Frontend first"], ["Draft sync", "Guarded", "Owner approved"], ["Paid status", "Refresh", "Accounting confirms"], ["Safety", "Locked", "No auto-send"]]} /></Panel></div>; }
function SettingsPage() { return <div className="cvxPageGrid"><Panel eyebrow="Business profile" title="Brand and defaults" wide><FieldForm button="Save settings later" fields={[["Business name", "Churvox"], ["Owner email", "hello@churvox.com"], ["GST rate", "15"], ["Country", "NZ"], ["Default repeat", "Fortnightly", "select", options.repeat], ["Default billing", "Fixed price", "select", options.billing], ["Logo", "Not uploaded"], ["Notifications", "On"]]} /></Panel><Panel eyebrow="Imports" title="CSV and records"><MiniList items={[["Clients CSV", "Visible from Settings and Clients"], ["Team CSV", "Invite workers from a file later"], ["Invoice export", "Bookkeeper CSV stays guarded"]]} /></Panel><Panel eyebrow="Security" title="Account controls"><MiniList items={[["Email verification", "Required before live use"], ["Forgot password", "Email service wiring later"], ["Delete account", "Control comes back when backend wiring starts"], ["Business isolation", "Each business stays separate"]]} /></Panel></div>; }
function PlansPage() { return <div className="cvxPlans">{planRows.map(([name, price, planText, bullets]) => <section key={name} className={name === "Operator" ? "cvxPopularPlan" : ""}>{name === "Operator" && <em>Most Popular</em>}<p className="cvxEyebrow">{name}</p><h2>{price}<small>/month + GST</small></h2><span>{planText}</span><ul>{bullets.map((line) => <li key={line}>{line}</li>)}</ul></section>)}</div>; }
function SupportPage() { return <div className="cvxPageGrid"><Panel eyebrow="Help" title="Churvox setup"><MiniList items={[["Start", "Add clients and jobs"], ["Command", "Approve what Churvox prepares"], ["Workers", "Use slips, map, proof and messages"], ["Accounting", "Keep draft sync guarded"]]} /></Panel><Panel eyebrow="Contact" title="hello@churvox.com"><p className="cvxNotice">Support wiring comes after the clean frontend is stable.</p></Panel><Panel eyebrow="Guides" title="What each page is for"><ul className="cvxRules"><li>AI Guide shows the business pulse.</li><li>Command is the owner approval desk.</li><li>Jobs and Clients hold the real work forms.</li><li>Workers is where map, proof and messages live.</li></ul></Panel></div>; }
function Page({ page, data, open }) { if (page === "aiguide") return <AIGuide data={data} />; if (page === "command") return <CommandPage data={data} open={open} />; if (page === "jobs") return <JobsPage data={data} open={open} />; if (page === "clients") return <ClientsPage data={data} open={open} />; if (page === "quotes") return <QuotesPage data={data} open={open} />; if (page === "invoices") return <InvoicesPage data={data} open={open} />; if (page === "team") return <TeamPage data={data} open={open} />; if (page === "payroll") return <PayrollPage data={data} open={open} />; if (page === "workers") return <WorkersPage data={data} open={open} />; if (page === "xero") return <XeroPage data={data} />; if (page === "settings") return <SettingsPage />; if (page === "plans") return <PlansPage />; return <SupportPage />; }
export default function FreshAppBackend() { const api = useApi(); const { data, reload } = useBackendData(api); const [page, setPage] = React.useState(startPage); const [selected, setSelected] = React.useState(null); const [title, sub] = titles[page] || titles.aiguide; React.useEffect(() => { const onHash = () => setPage(startPage()); window.addEventListener("hashchange", onHash); return () => window.removeEventListener("hashchange", onHash); }, []); const go = (next) => { setPage(next); setSelected(null); if (typeof window !== "undefined") window.history.replaceState({}, "", `/dashboard#${next}`); }; return <main className="cvxClean"><div className="cvxCleanShell"><header className="cvxCleanTop"><div className="cvxCleanHead"><div className="cvxCleanBrand"><span className="cvxCleanMark" /><span><b>Churvox</b><small>does the admin</small></span></div><div className="cvxCleanTitle"><h1>{title}</h1><p>{sub}</p></div><a className="cvxCleanLogout" href="/login">Log out</a></div><nav className="cvxCleanNav" aria-label="Churvox pages">{nav.map((item) => { const next = key(item); return <button key={item} type="button" className={page === next ? "active" : ""} onClick={() => go(next)}>{item}</button>; })}</nav></header>{data.backend !== "live" ? <p className="cvxNotice">Showing safe example records until backend data is available.</p> : null}<section className="cvxCleanWork"><Page page={page} data={data} open={setSelected} /></section></div><Drawer item={selected} page={page} api={api} reload={reload} onClose={() => setSelected(null)} /></main>; }
