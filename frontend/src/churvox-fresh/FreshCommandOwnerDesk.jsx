import React from "react";
import { toast } from "sonner";
import { useApi } from "../hooks/useApi";

const REVIEW_KEY = "churvox:review-inbox:v1";
const OLD_REVIEW_KEY = "churvox:fresh-command-inbox:v1";
const ARCHIVE_KEY = "churvox:review-archive:v1";

function safeParse(value, fallback = []) { try { const parsed = JSON.parse(value || "[]"); return Array.isArray(parsed) ? parsed : fallback; } catch { return fallback; } }
function titleOf(item) { return item?.title || item?.summary || "Prepared admin action"; }
function summaryOf(item) { const d = item?.details || {}; return item?.summary || d.Change || d.Action || d.Status || "Prepared by Churvox and waiting for owner approval."; }
function categoryOf(item) { const raw = String(item?.category || item?.type || "").toLowerCase(); if (raw.includes("invoice") || raw.includes("money") || raw.includes("chase")) return "money"; if (raw.includes("job") || raw.includes("complete") || raw.includes("reschedule") || raw.includes("update") || raw.includes("find")) return "work"; if (raw.includes("client") || raw.includes("quote") || raw.includes("person") || raw.includes("worker") || raw.includes("create")) return "create"; return "other"; }
function entriesOf(item) { const entries = Object.entries(item?.details || {}).filter(([, value]) => value !== undefined && value !== null && String(value).trim()); return entries.length ? entries : [["What Churvox found", item?.livePreview?.previewTitle || "Saved review item"], ["What Churvox prepared", summaryOf(item)], ["Why it needs approval", "Nothing changes until the owner approves."]]; }
function loadItems() { const byId = new Map(); [...safeParse(window.localStorage.getItem(REVIEW_KEY)), ...safeParse(window.localStorage.getItem(OLD_REVIEW_KEY))].forEach((item) => { if (!item || typeof item !== "object") return; const id = item.id || `${item.title || "review"}-${item.createdAt || ""}`; if (!byId.has(id)) byId.set(id, { ...item, id, category: categoryOf(item) }); }); return Array.from(byId.values()).filter((item) => !["approved", "ignored", "archived"].includes(String(item.status || ""))); }
function saveItems(items) { window.localStorage.setItem(REVIEW_KEY, JSON.stringify(items)); window.localStorage.setItem(OLD_REVIEW_KEY, JSON.stringify(items)); window.dispatchEvent(new CustomEvent("churvox:fresh-data-updated", { detail: { type: "review-tray" } })); }
function archiveItem(item, outcome) { const current = safeParse(window.localStorage.getItem(ARCHIVE_KEY)); window.localStorage.setItem(ARCHIVE_KEY, JSON.stringify([{ ...item, outcome, archivedAt: new Date().toISOString() }, ...current].slice(0, 100))); }
function amountFrom(value) { const raw = String(value || ""); const money = raw.match(/\$\s*(\d+(?:\.\d{1,2})?)/); if (money) return Number(money[1]); const number = raw.match(/\b(\d+(?:\.\d{1,2})?)\b/); return number ? Number(number[1]) : 0; }
function valueOf(details, keys, fallback = "") { for (const key of keys) { const value = details?.[key]; if (value !== undefined && value !== null && String(value).trim() && !/optional|needed|date needed|price needed/i.test(String(value))) return String(value).trim(); } return fallback; }
function firstMatch(item) { const live = item?.livePreview || {}; return live.bestMatch || live.matches?.[0] || null; }
function jobAmount(match, fallback = 0) { const record = match?.record || match || {}; return Number(match?.amount || record.price || record.fixed_price || record.total || record.subtotal || record.amount || fallback || 0); }
function inferJobType(text) { const low = String(text || "").toLowerCase(); if (low.includes("hedge")) return "hedge_trimming"; if (low.includes("clean")) return "cleaning"; if (low.includes("lawn") || low.includes("mow")) return "lawn_mowing"; if (low.includes("paint")) return "painting"; if (low.includes("pest")) return "pest_control"; if (low.includes("plumb")) return "plumbing"; if (low.includes("elect")) return "electrical"; return "other"; }
function scheduleISO(value) { const raw = String(value || "").trim().toLowerCase(); const base = new Date(); if (raw.includes("today")) return base.toISOString(); if (raw.includes("tomorrow")) { base.setDate(base.getDate() + 1); return base.toISOString(); } if (raw.includes("next week")) { base.setDate(base.getDate() + 7); return base.toISOString(); } const match = raw.match(/\b(\d{1,2})[/.-](\d{1,2})(?:[/.-](\d{2,4}))?\b/); if (match) { const day = Number(match[1]); const month = Number(match[2]) - 1; let year = match[3] ? Number(match[3]) : base.getFullYear(); if (year < 100) year += 2000; const parsed = new Date(year, month, day, 9, 0, 0); if (!Number.isNaN(parsed.getTime())) return parsed.toISOString(); } base.setDate(base.getDate() + 1); return base.toISOString(); }
function invoicePayloadFromJob(match, fallbackAmount = 0) { const record = match?.record || match || {}; const amount = jobAmount(match, fallbackAmount); const clientName = record.client_name || record.customer_name || match?.label || "Customer"; const description = record.title || record.job_name || record.description || "Completed job"; return { customer_name: clientName, customer_email: record.customer_email || record.client_email || null, address: record.address || record.site_address || "", description, notes: `Draft invoice prepared from Review for ${match?.label || description}.`, status: "draft", subtotal: amount, job_id: match?.id || record.id || record._id || null };
}
async function postFirst(post, endpoints, payload) { let lastError = "Action failed"; for (const endpoint of endpoints) { const res = await post(endpoint, payload, { timeout: 20000 }); if (res?.success) return res; lastError = res?.error || lastError; } return { success: false, error: lastError }; }
function createPlan(item) { const d = item?.details || {}; const type = String(item?.type || "").toLowerCase(); const amount = amountFrom(valueOf(d, ["Price", "Amount"])); const client = valueOf(d, ["Client", "Client / target"], "Customer"); const line = valueOf(d, ["Line", "Scope", "Job", "Action"], "General service"); const address = valueOf(d, ["Address"], "Address needed"); const email = valueOf(d, ["Email"]); const phone = valueOf(d, ["Phone"]); if (type === "client") return { endpoints: ["/clients"], payload: { name: client, email: email || null, phone: phone || null, address, notes: summaryOf(item) } }; if (type === "person" || type === "worker") return { endpoints: ["/team/workers", "/team", "/workers"], payload: { name: valueOf(d, ["Worker"], client), email: email || null, phone: phone || null, role: "worker", team_role: "worker", notes: summaryOf(item) } }; if (type === "quote") return { endpoints: ["/quotes"], payload: { customer_name: client, customer_email: email || null, address, job_description: line, price: amount, notes: summaryOf(item) } }; if (type === "invoice") return { endpoints: ["/invoices"], payload: { customer_name: client, customer_email: email || null, address, description: line, subtotal: amount || 0, notes: summaryOf(item), status: "draft" } }; return { endpoints: ["/jobs"], payload: { title: `${line} for ${client}`, job_type: inferJobType(`${line} ${summaryOf(item)}`), customer_name: client, address, scheduled_date: scheduleISO(valueOf(d, ["Schedule", "Date", "Change"])), notes: summaryOf(item), status: "assigned", pricing_type: "fixed", price: amount || 0 } }; }
function DetailGrid({ item }) { return <div className="freshReviewDetailGrid">{entriesOf(item).slice(0, 8).map(([key, value]) => <section key={key}><b>{key}</b><p>{String(value)}</p></section>)}</div>; }
function ReviewModal({ item, busy, onClose, onSave, onApprove, onIgnore }) { const [note, setNote] = React.useState(item?.ownerNote || ""); React.useEffect(() => { setNote(item?.ownerNote || ""); }, [item?.id]); if (!item) return null; return <div className="freshReviewShade" role="dialog" aria-modal="true"><section className="freshReviewModal"><button className="freshReviewClose" type="button" onClick={onClose}>×</button><header><span>Owner Review</span><h2>{titleOf(item)}</h2><p>{summaryOf(item)}</p></header><DetailGrid item={item} /><section className="freshReviewSafe"><b>Safe rule</b><p>{item.category === "money" ? "Money actions stay draft-only. Nothing sends, syncs or marks paid from Review." : item.category === "work" ? "Job changes only happen after you approve the matched action." : "Create actions can be checked before they become real records."}</p></section><label className="freshReviewNote"><span>Owner note / edit</span><textarea value={note} onChange={(event) => setNote(event.target.value)} placeholder="Add a note or reason before saving." /></label><div className="freshActions"><button className="freshPrimary" type="button" disabled={busy} onClick={() => onApprove(item, { note })}>{busy ? "Working…" : "Approve"}</button><button className="freshGhost" type="button" disabled={busy} onClick={() => onSave(item, { note })}>Save edit</button><button className="freshGhost" type="button" disabled={busy} onClick={() => onIgnore(item, { note })}>Ignore</button></div></section></div>; }
function ReviewCard({ item, busy, onOpen, onApprove, onIgnore }) { return <article className={`freshReviewItem ${item.category || "other"}`}><div><span>{item.category || "review"}</span><em>{item.createdAt || "now"}</em></div><h3>{titleOf(item)}</h3><p>{summaryOf(item)}</p><DetailGrid item={item} /><div className="freshActions"><button className="freshPrimary" type="button" onClick={() => onOpen(item)}>Open review</button><button className="freshGhost" type="button" disabled={busy} onClick={() => onApprove(item, { note: "Approved from Review." })}>Approve</button><button className="freshGhost" type="button" disabled={busy} onClick={() => onIgnore(item, { note: "Ignored from Review." })}>Ignore</button></div></article>; }
function Style() { return <style>{`.freshReviewPage{display:grid;gap:16px}.freshReviewHero{border-radius:34px;background:#101827;color:white;padding:28px;border-left:8px solid #f97316;box-shadow:0 24px 70px rgba(2,6,23,.20)}.freshReviewHero span{display:inline-flex;border-radius:999px;background:#fff7ed;color:#9a3412;padding:9px 13px;font-size:11px;font-weight:1000;text-transform:uppercase;letter-spacing:.12em}.freshReviewHero h1{margin:13px 0 8px;color:white;font-size:clamp(38px,5vw,66px);line-height:.92;letter-spacing:-.06em}.freshReviewHero p{margin:0;color:#e5e7eb;font-weight:850;line-height:1.45}.freshReviewStats{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:10px;margin-top:18px}.freshReviewStats div{border-radius:18px;background:rgba(255,255,255,.08);border:1px solid rgba(255,255,255,.12);padding:13px}.freshReviewStats small{display:block;color:#fed7aa;font-size:10px;font-weight:1000;text-transform:uppercase;letter-spacing:.12em}.freshReviewStats b{display:block;color:#fff;font-size:25px;margin-top:4px}.freshReviewToolbar{display:flex;flex-wrap:wrap;gap:8px}.freshReviewToolbar button{border:1px solid rgba(15,23,42,.12);border-radius:999px;background:#fffaf0;color:#101827;padding:10px 14px;font-weight:1000;cursor:pointer}.freshReviewToolbar button.active{background:#111827;color:white}.freshReviewList{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:14px}.freshReviewItem{border:1px solid rgba(15,23,42,.12);border-left:7px solid #f97316;border-radius:28px;background:#fffaf0;padding:17px;box-shadow:0 16px 42px rgba(2,6,23,.08);display:grid;gap:11px}.freshReviewItem.money{border-left-color:#16a34a}.freshReviewItem.work{border-left-color:#2563eb}.freshReviewItem>div:first-child{display:flex;justify-content:space-between;gap:10px}.freshReviewItem span{border-radius:999px;background:#fff7ed;color:#9a3412;padding:7px 10px;text-transform:uppercase;letter-spacing:.12em;font-size:10px;font-weight:1000}.freshReviewItem em{font-style:normal;color:#64748b;font-size:11px;font-weight:900}.freshReviewItem h3{margin:0;color:#101827;font-size:24px;line-height:1;letter-spacing:-.04em}.freshReviewItem p{margin:0;color:#475569;font-weight:850;line-height:1.4}.freshReviewDetailGrid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:8px}.freshReviewDetailGrid section{border:1px solid rgba(15,23,42,.10);border-radius:16px;background:#fff;padding:10px}.freshReviewDetailGrid b{display:block;color:#101827;font-size:12px;font-weight:1000}.freshReviewDetailGrid p{margin:4px 0 0;color:#475569;font-size:12px;font-weight:850;word-break:break-word}.freshReviewEmpty{border:1px dashed rgba(15,23,42,.18);border-radius:28px;background:#fffaf0;padding:26px;display:grid;gap:12px}.freshReviewEmpty h2{margin:0;color:#101827;font-size:30px;letter-spacing:-.04em}.freshReviewEmpty p{margin:0;color:#475569;font-weight:850;max-width:780px}.freshReviewShade{position:fixed;inset:0;background:rgba(2,6,23,.60);z-index:9999;display:grid;place-items:center;padding:18px}.freshReviewModal{max-width:980px;width:min(980px,100%);max-height:90vh;overflow:auto;border-radius:30px;background:#fffaf0;padding:22px;box-shadow:0 30px 90px rgba(2,6,23,.35);display:grid;gap:14px;position:relative}.freshReviewClose{position:absolute;right:14px;top:14px;border:0;border-radius:999px;width:40px;height:40px;background:#111827;color:#fff;font-size:22px;cursor:pointer}.freshReviewModal header span{display:inline-flex;border-radius:999px;background:#fff7ed;color:#9a3412;padding:8px 11px;font-size:10px;font-weight:1000;text-transform:uppercase;letter-spacing:.12em}.freshReviewModal h2{margin:9px 0 4px;color:#101827;font-size:34px;letter-spacing:-.05em}.freshReviewModal p{margin:0;color:#475569;font-weight:850}.freshReviewSafe{border:1px solid rgba(249,115,22,.22);border-radius:20px;background:#fff7ed;padding:13px}.freshReviewSafe b{color:#9a3412}.freshReviewSafe p{margin-top:5px}.freshReviewNote{display:grid;gap:7px}.freshReviewNote span{font-weight:1000;color:#101827}.freshReviewNote textarea{min-height:110px;border:1px solid rgba(15,23,42,.14);border-radius:18px;padding:12px;font-weight:850;color:#101827;background:white}@media(max-width:800px){.freshReviewStats,.freshReviewList,.freshReviewDetailGrid{grid-template-columns:1fr}.freshReviewHero{padding:22px}.freshReviewHero h1{font-size:42px}}`}</style>; }
export default function FreshCommandOwnerDesk({ onNavigate }) { const { post, patch } = useApi(); const [items, setItems] = React.useState(loadItems); const [filter, setFilter] = React.useState("all"); const [active, setActive] = React.useState(null); const [busyId, setBusyId] = React.useState(""); React.useEffect(() => { const reload = () => setItems(loadItems()); window.addEventListener("storage", reload); window.addEventListener("churvox:fresh-data-updated", reload); return () => { window.removeEventListener("storage", reload); window.removeEventListener("churvox:fresh-data-updated", reload); }; }, []); function removeItem(item, outcome) { const next = items.filter((entry) => entry.id !== item.id); archiveItem(item, outcome); saveItems(next); setItems(next); setActive(null); } async function approveItem(item, extra = {}) { if (!item || item.status === "guide") return; setBusyId(item.id); try { const rawCategory = String(item.category || "").toLowerCase(); const d = item.details || {}; let result = { success: true }; if (rawCategory === "create") { const plan = createPlan(item); result = await postFirst(post, plan.endpoints, plan.payload); } else if (rawCategory === "invoice_job") { const match = firstMatch(item); const fallback = amountFrom(valueOf(d, ["Change", "Amount", "Price"])); if (!match) throw new Error("No matched job found for this draft invoice."); result = await post("/invoices", invoicePayloadFromJob(match, fallback), { timeout: 20000 }); } else if (rawCategory === "invoice_batch") { const matches = item?.livePreview?.matches || []; if (!matches.length) throw new Error("No completed jobs found for batch invoices."); for (const match of matches) { const res = await post("/invoices", invoicePayloadFromJob(match), { timeout: 20000 }); if (!res?.success && !res?.duplicate) throw new Error(res?.error || "One draft invoice could not be created."); } result = { success: true }; } else if (rawCategory === "complete" || rawCategory === "reschedule" || rawCategory === "update") { const match = firstMatch(item); const id = match?.id || match?.record?.id || match?.record?._id; if (!id) throw new Error("No matched job found for this change."); const changeText = valueOf(d, ["Change", "Schedule"]); const priceAmount = amountFrom(valueOf(d, ["Change", "Price", "Amount"])); const payload = rawCategory === "complete" ? { status: "completed", completed_at: new Date().toISOString() } : rawCategory === "reschedule" ? { scheduled_date: scheduleISO(changeText), notes: extra.note || summaryOf(item) } : priceAmount ? { price: priceAmount, notes: extra.note || summaryOf(item) } : { notes: extra.note || summaryOf(item) }; result = await patch(`/jobs/${id}`, payload, { timeout: 20000 }); } else if (rawCategory === "chase_invoices") { result = { success: true }; } else { const plan = createPlan(item); result = await postFirst(post, plan.endpoints, plan.payload); } if (!result?.success && !result?.duplicate) throw new Error(result?.error || "Approval failed."); removeItem({ ...item, ownerNote: extra.note || "", approvedAt: new Date().toISOString() }, "approved"); toast.success(result?.duplicate ? "Already handled — no duplicate created" : "Approved. Churvox saved the prepared work."); } catch (err) { toast.error(err?.message || "Could not approve this item."); } finally { setBusyId(""); } } function saveEdit(item, extra = {}) { const next = items.map((entry) => entry.id === item.id ? { ...entry, ownerNote: extra.note || "", status: "edited" } : entry); saveItems(next); setItems(next); setActive(null); toast.success("Saved edit. Nothing changed live yet."); } function ignoreItem(item, extra = {}) { removeItem({ ...item, ownerNote: extra.note || "", ignoredAt: new Date().toISOString() }, "ignored"); toast.info("Ignored. Nothing changed."); } const waiting = items.length; const money = items.filter((item) => item.category === "money").length; const work = items.filter((item) => item.category === "work").length; const create = items.filter((item) => item.category === "create").length; const visible = filter === "all" ? items : items.filter((item) => item.category === filter); return <section className="freshReviewPage"><Style /><header className="freshReviewHero"><span>Owner Review</span><h1>Approve what Churvox prepared.</h1><p>This is the holding tray for AI prepared admin work. Nothing sends, syncs, marks paid or changes live records without your decision.</p><div className="freshReviewStats"><div><small>Waiting</small><b>{waiting}</b></div><div><small>Money</small><b>{money}</b></div><div><small>Work</small><b>{work}</b></div><div><small>Create</small><b>{create}</b></div></div></header><div className="freshReviewToolbar">{["all", "money", "work", "create", "other"].map((key) => <button key={key} type="button" className={filter === key ? "active" : ""} onClick={() => setFilter(key)}>{key}</button>)}</div>{waiting === 0 ? <section className="freshReviewEmpty"><h2>Nothing waiting for approval.</h2><p>Tell Churvox what happened and prepared admin will appear here. This page is a real decision tray, not a demo board.</p><div className="freshActions"><button className="freshPrimary" type="button" onClick={() => onNavigate?.("quickcreateai")}>Tell Churvox</button><button className="freshGhost" type="button" onClick={() => onNavigate?.("smart")}>Back to Today</button></div></section> : <section className="freshReviewList">{visible.map((item) => <ReviewCard key={item.id} item={item} busy={busyId === item.id} onOpen={setActive} onApprove={approveItem} onIgnore={ignoreItem} />)}</section>}<ReviewModal item={active} busy={busyId === active?.id} onClose={() => setActive(null)} onApprove={approveItem} onSave={saveEdit} onIgnore={ignoreItem} /></section>; }
