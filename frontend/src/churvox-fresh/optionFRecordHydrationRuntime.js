// Disabled: this old hydration runtime injected extra panels into every FreshApp page and fought the real owner workbench.
// FreshApp now owns the page records, drawers, and Command approval flow directly.
export {};
