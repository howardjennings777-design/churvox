// CHURVOX_DISABLE_PLANS_NAV_INJECTION_20260530
// /plans still exists, but the Plans link is no longer injected into app nav.
// It made the Command bottom dock wrap into two rows.

export default null;
