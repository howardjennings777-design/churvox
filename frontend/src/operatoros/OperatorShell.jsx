import FloatingLogo from './components/FloatingLogo';import TopCommandBar from './components/TopCommandBar';
export default function OperatorShell({nav,current,setCurrent,children,role,setRole}){return <div className='op-shell'><aside className='op-sidebar'><FloatingLogo small/><h3>Churvox Operator OS</h3>{nav.map(n=><button key={n.key} className={current===n.key?'active':''} onClick={()=>setCurrent(n.key)}>{n.label}</button>)}</aside><main className='op-main'><TopCommandBar role={role} setRole={setRole}/>{children}</main><nav className='op-mobile-nav'>{nav.filter(n=>['hub','queue','jobs','invoices'].includes(n.key)).map(n=><button key={n.key} onClick={()=>setCurrent(n.key)}>{n.mobile||n.label}</button>)}<button onClick={()=>setCurrent('settings')}>More</button></nav></div>;}
