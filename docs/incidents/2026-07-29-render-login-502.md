# Intermittent Render login 502 — 29 July 2026

## Observed

During PR #973 validation, the production frontend build, backend syntax, launch contracts, owner Studio render, Worker Proof singleton contract and conversion-funnel gate passed.

Authenticated readiness and paid-launch browser checks intermittently failed because the same-origin API proxy received HTTP 502 responses from the live Render backend during owner login. One commercial-loop attempt also timed out waiting for a converted quote to appear while the backend was unstable.

## Impact

- Owner login can remain on the login page while the backend is unavailable.
- Worker discovery and cleanup cannot authenticate during the outage.
- Live mutation tests may fail even when the branch build and isolated contracts are healthy.

## Follow-up

1. Review Render service health, restarts, memory and request logs around 2026-07-28 23:00–23:08 UTC.
2. Verify `/api/auth/login`, session confirmation and quote-conversion endpoints after the service stabilises.
3. Re-run the Product Readiness Gate and Paid Launch Gate.
4. Keep the owner-login retry test strict; do not add worker-login fallback or weaken the gate.

## Related

- PR #973 — Get Work, Churvox Guard, funnel tracking and worker proof flow.
