name: Churvox Launch Ready Check

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  launch-ready:
    runs-on: ubuntu-latest
    timeout-minutes: 35

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"
          cache-dependency-path: frontend/package-lock.json

      - name: Backend syntax check
        run: |
          python3 -m py_compile backend/*.py
          python3 -m compileall -q backend

      - name: Verify required launch boot files
        run: |
          test -f backend/sitecustomize.py
          test -f backend/top_player_boot.py
          test -f backend/notifications_boot.py
          test -f backend/recurring_jobs_boot.py
          test -f backend/job_invoice_boot.py
          test -f backend/quote_job_boot.py
          test -f backend/client_360_boot.py
          test -f backend/checklist_automation_boot.py
          test -f backend/launch_audit_boot.py
          test -f backend/launch_ops_boot.py

      - name: Install frontend dependencies
        working-directory: frontend
        run: npm install --legacy-peer-deps

      - name: Build frontend
        working-directory: frontend
        env:
          CI: false
        run: npm run build

      - name: Install Playwright Chromium
        working-directory: frontend
        run: npx playwright install --with-deps chromium

      - name: Run Playwright live launch audit
        working-directory: frontend
        env:
          CHURVOX_AUDIT_URL: https://www.churvox.com
          CHURVOX_AUDIT_EMAIL: ${{ secrets.CHURVOX_AUDIT_EMAIL }}
          CHURVOX_AUDIT_PASSWORD: ${{ secrets.CHURVOX_AUDIT_PASSWORD }}
        run: npm run audit:launch

      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: churvox-playwright-report
          path: frontend/playwright-report
          if-no-files-found: ignore

      - name: Upload Playwright traces
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: churvox-playwright-test-results
          path: frontend/test-results
          if-no-files-found: ignore

      - name: Upload frontend build artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: churvox-frontend-build
          path: frontend/build
          if-no-files-found: ignore
