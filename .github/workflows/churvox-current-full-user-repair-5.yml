name: Churvox Forced Auth Resilience Repair

on:
  issues:
    types: [opened]

permissions:
  contents: write
  issues: write

jobs:
  repair:
    if: startsWith(github.event.issue.title, '[churvox-current-full-user-force-fix]')
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Apply forced auth resilience repair
        id: apply
        shell: bash
        run: |
          set -o pipefail
          python scripts/churvox_current_full_user_force_fix.py 2>&1 | tee /tmp/force-fix-apply.log

      - name: Verify exact product and audit contracts
        id: verify
        shell: bash
        run: |
          set -o pipefail
          (
            set -euo pipefail
            node --check frontend/tests/e2e/churvox-login-recovery-paid-launch.spec.js
            node --check frontend/tests/e2e/churvox-live-launch-human-audit-v2.spec.js
            node --check frontend/tests/e2e/churvox-current-human-owner-worker-flow.spec.js
            node --check frontend/tests/e2e/churvox-hardcore-owner-worker-visual.spec.js
            node --check scripts/churvox-hardcore-human-cleanup.cjs
            grep -F 'const AUTH_TIMEOUT_MS = 30000;' frontend/src/context/AuthContext.js
            grep -F 'async function withTransientAuthRetry(operation, attempts = 4)' frontend/src/context/AuthContext.js
            grep -F 'return [401, 404, 422].includes(status);' frontend/src/context/AuthContext.js
            grep -F 'const LOGIN_TIMEOUT_MS = 180000;' frontend/src/pages/auth/LoginPage.js
            grep -F 'const user = await checkAuth({ allowOfflineFallback: false });' frontend/src/pages/auth/LoginPage.js
            grep -F 'try { void logout?.(); } catch {}' frontend/src/pages/auth/LoginPage.js
            grep -F '[408, 425, 502, 503, 504].includes(status)' frontend/src/pages/auth/LoginPage.js
            grep -F 'loginFailures: 2' frontend/tests/e2e/churvox-login-recovery-paid-launch.spec.js
            grep -F 'toBeGreaterThanOrEqual(3)' frontend/tests/e2e/churvox-login-recovery-paid-launch.spec.js
            grep -F 'attempt <= 6' frontend/tests/e2e/churvox-live-launch-human-audit-v2.spec.js
            grep -F 'attempt <= 6' frontend/tests/e2e/churvox-current-human-owner-worker-flow.spec.js
            grep -F 'attempt <= 6' frontend/tests/e2e/churvox-hardcore-owner-worker-visual.spec.js
            grep -F 'CHURVOX_CLEANUP_REQUEST_TIMEOUT_MS || 45_000' scripts/churvox-hardcore-human-cleanup.cjs
            grep -F 'CHURVOX_CLEANUP_ATTEMPTS || 6' scripts/churvox-hardcore-human-cleanup.cjs
            grep -F 'churvox-current-full-user-force-fix-20260728' frontend/public/render-deploy-marker.txt
            node scripts/churvox-hardcore-owner-worker-audit.cjs
          ) 2>&1 | tee /tmp/force-fix-verify.log

      - name: Install and build production frontend
        id: build
        shell: bash
        run: |
          set -o pipefail
          (
            set -euo pipefail
            npm --prefix frontend install --include=dev --legacy-peer-deps
            npm --prefix frontend run build
          ) 2>&1 | tee /tmp/force-fix-build.log

      - name: Run complete login and recovery browser contract
        id: browser
        working-directory: frontend
        shell: bash
        run: |
          set -o pipefail
          (
            set -euo pipefail
            npx playwright install --with-deps chromium
            npm start > /tmp/force-fix-server.log 2>&1 &
            for i in {1..60}; do
              if curl -fsS http://127.0.0.1:3000/login >/dev/null; then break; fi
              sleep 2
            done
            PLAYWRIGHT_BASE_URL=http://127.0.0.1:3000 npx playwright test tests/e2e/churvox-login-recovery-paid-launch.spec.js --project=desktop-chromium --workers=1 --reporter=line
          ) 2>&1 | tee /tmp/force-fix-browser.log

      - name: Commit verified forced repair
        id: commit
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add \
            frontend/public/render-deploy-marker.txt \
            frontend/src/context/AuthContext.js \
            frontend/src/pages/auth/LoginPage.js \
            frontend/tests/e2e/churvox-login-recovery-paid-launch.spec.js \
            frontend/tests/e2e/churvox-live-launch-human-audit-v2.spec.js \
            frontend/tests/e2e/churvox-current-human-owner-worker-flow.spec.js \
            frontend/tests/e2e/churvox-hardcore-owner-worker-visual.spec.js \
            scripts/churvox-hardcore-human-cleanup.cjs
          git commit -m 'Force fix live authentication resilience'
          git push origin HEAD:main

      - name: Post exact result
        if: always()
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const outcomes = {
              apply: '${{ steps.apply.outcome }}',
              verify: '${{ steps.verify.outcome }}',
              build: '${{ steps.build.outcome }}',
              browser: '${{ steps.browser.outcome }}',
              commit: '${{ steps.commit.outcome }}',
            };
            const ok = '${{ job.status }}' === 'success';
            let body;
            if (ok) {
              body = 'Forced auth resilience repair passed exact source guards, permanent owner/worker contracts, the production build and the complete login/recovery browser suite. The double post-login retry race is removed, temporary Render gateway failures retry the correct role endpoint, outage messages appear immediately, and exact-run cleanup is safely retried. Verified changes were committed to main with the force-fix marker.';
            } else {
              const order = [['apply','/tmp/force-fix-apply.log'], ['verify','/tmp/force-fix-verify.log'], ['build','/tmp/force-fix-build.log'], ['browser','/tmp/force-fix-browser.log']];
              const failed = order.find(([name]) => outcomes[name] === 'failure');
              let tail = '';
              if (failed && fs.existsSync(failed[1])) tail = fs.readFileSync(failed[1], 'utf8').split('\n').slice(-80).join('\n').slice(-12000);
              body = `Forced auth resilience repair stopped before commit. Exact outcomes: ${JSON.stringify(outcomes)}. No partial product repair was pushed.${tail ? `\n\nFailed-stage log tail:\n\`\`\`text\n${tail}\n\`\`\`` : ''}`;
            }
            await github.rest.issues.createComment({ owner: context.repo.owner, repo: context.repo.repo, issue_number: context.issue.number, body });
